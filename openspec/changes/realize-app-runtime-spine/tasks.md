## 1. Seal Authority And Migration Input

- [x] 1.1 Checkpoint the reviewed canonical candidate directly above predecessor `3c84248e` at commit `2422459b09adff29fd55b9a406a8423f45240774`, with `docs/system/HABITAT_ARCHITECTURE.md` blob `3d86bb9cfb3bafb522a356ea84d026b78827fbd6` and `docs/system/HABITAT_RUNTIME_REALIZATION.md` blob `3cf41d061e1cba7c32a8e52492985dd8d01d2570`; retain the frozen lineage `d4acaa7f` through `a1e6e4c6` and blobs `2961da49` / `c6f475cc` as source provenance rather than alternate current authority. The old Rawr-named project paths are provenance pointers and cannot satisfy this gate.
- [x] 1.2 Correct the whole-board Nx/app/runtime inventory in [[classification-ledger]] and record the exact Habitat-platform, proven Rawr-product, qualified fixture, or delete destination for every mixed core, app, support package, service, resource/provider, tool, policy, test, and CLI command owner; treat no current name or path as ownership evidence. Inventory every visible Graphite stack in [[stack-cut-sheet]] and give it exactly one disposition: retain/restack/merge in Habitat, finite owner transfer to Rawr or Marketplace, retire, or externally owned/held. Record `apps/habitat` only as the composition destination for qualified non-core Habitat capabilities, not as a fifth classification.
- [x] 1.3 Identify the latest applicable Runtime Realization Lab as commit `3147acbd`, whole tree `7fff3eaf6d80a4609dd0d511696212a38133753d`, and `tools/runtime-realization-type-env` subtree `d35cd11d`; classify reusable algorithms and fixtures and reject its stale public/runtime shapes.
- [x] 1.4 Record Magic Migration clean blueprint snapshot `4e2f5d63` / tree `88f0f24e` and latest applicable committed implementation `c4d9aa83` / tree `f062e173`; admit only its abstract app boundary, service-context, scoped-resource, and direct-face mechanics, exclude the dirty worktree and product-local wiring, and record its missing canonical phases and telemetry proof.
- [x] 1.5 Land the reviewed parent from task 1.1 on current `main` at merge commit `f920232efbcb10cc4a7220e3b6be4b81a393009d` and land this execution record directly above it at `7457505fc5dc068c1ff80a06ca78f713ebe3a954`. Treat the landed document contents as semantic authority and the task 1.1 source/blob identities only as ordinary Git provenance; no branch name, transient restack commit, or byte comparison becomes a second authority.
- [x] 1.6 Obtain independent architecture, TypeScript, structural-quality, and behavior-first test reviews of the Habitat-platform/Rawr-product recut OpenSpec; resolve every P0/P1 before source implementation opens.
- [x] 1.7 Record the latest committed Magic consumer oracle at `ec7a49c596ca50d5c8ef8ce3f8e3e40cb08c33a7`, whole tree `2b3c99700d5db8264b7ee42910575e8b877bda3a`, separately from the clean blueprint snapshot `4e2f5d63` / tree `88f0f24e`. Freeze only generic assertions: one semantic app may select independently started server and async process records; each process owns its lease, resources, native lifetime, stop, and health identity; required-resource failure precedes mount; and sibling stop/restart remains independent. Magic's product wiring, direct provider construction, Railway placement, copied `mcp-openapi` tarball, and dirty deployment evidence remain excluded.

## 2. Define Generic Law Without A Red Landing

- [x] 2.0 Before Gate A, land the minimum blueprint-definition authoring fix in
  this order. **(a)** Keep the existing schema-version-1 `blueprint.toml`
  `[[rules]]` sequence as the only ordered composition surface. Treat a
  component as informal shorthand for a named authoring subdirectory. For the
  Gate A service proof, keep exactly one root Habitat structure rule whose
  `structure.toml` owns the complete positive filesystem closure; group only
  focused ordinary Grit `pattern.md` assets beneath the component directories;
  reference them through existing rule runners; and preserve every rule's
  ordinary application and focused target. Retain one
  project-root `habitat.toml`, one selected blueprint/version, one Nx owner, and
  owner `check:policy` composition per instance. Restore recursive
  source-to-installed byte parity for every rule-referenced authority asset
  instead of the shallow `${id}/*` copy; this recursive pack correction is the
  only engine/build change. Add no `components` definition field,
  schema version, runner-array form, runtime DTO, parser branch, evaluator merge,
  downstream configuration, component application, or component-named target;
  registry `@habitat-ai/cli@0.5.2` MUST remain able to project the repository
  before candidate publication. **(b)** Record `include` / `contains` only as a
  separately gated future extension. Gate A MUST implement zero blueprint
  relations. Any later relation slice must prove independently constructible
  reusable child law, create no child manifest/project/target, batch repeated
  occurrences of one related rule into one parent-owned application, and fail
  closed on missing definitions, cycles, escaping binds, overlaps, and
  cardinality violations. **(c)** Recast `service@1` as the first proof using
  the root structure anchor rule plus small ordinary rule packets grouped under
  the context-bearing `contract`, `funnel`, and `spine` authoring directories.
  Keep project, `client.ts`, and the source boundary in the root anchor; keep
  optional database law inside service-owned persistence; retain every packet's
  normal application and focused target; assign TypeScript, Nx, and behavior to
  their proper truths; and delete the current monolithic roughly 900-line
  source-law direction rather than optimizing it. Preserve the one-operation
  `nx add @habitat-ai/cli` consumer path. Add no inheritance, variants,
  capabilities, niches, manual inventories, custom runner, custom parser, open
  scopes, or downstream author configuration expansion.
- [ ] 2.1 Reconcile Habitat app law with Magic's abstract app boundary and record one semantic app/Nx project, profile, cold finite `ProcessCatalog`, process record, adapter, and harness model without product names. Preserve immutable `app@1` byte-for-byte and author complete `app@2` later as an independently resolvable successor with no inheritance or fallback. Distinguish the Habitat self-host from downstream products in project metadata; require `apps/<app-id>/<app-id>.app.ts`, `runtime/processes.ts`, and each thin mount/process-role entrypoint module to produce exactly one `Entrypoint` through `defineEntrypoint(...)`, thereby selecting one catalog process record, then pass that exact artifact to its sole future `startApp(...)` call without reconstructing selection or identity; permit surface suffixes only for single-surface mounts. A process is not an app, blueprint kind, child Nx project, supervisor, deployment unit, or sibling lifecycle owner. Record `scripts/habitat` as a Habitat tool rather than an application. No Rawr product app or predecessor `@habitat-ai/rawr`, `@rawr/hq-app`, `@rawr/server`, or `@rawr/web` identity may survive in Habitat.
- [ ] 2.2 Author closed blueprint packets and mental-model skill documents for the exact ten private runtime owners: `runtime-schema`, `runtime-definition`, `runtime-derivation`, `runtime-compiler`, `runtime-bootgraph`, `runtime-substrate-effect`, `runtime-process-runtime`, `runtime-harnesses`, `runtime-observation`, and `runtime-mounting`; retain schema-version-1 ordered ordinary rules, use blueprint-internal directories only as informal authoring organization, preserve each packet's ordinary application and target, require separate blueprint-specific authority before adding any extra structure rule/application, and express adapter law only as an owner-local packet within the process-runtime or harness blueprint rather than as a generic adapter blueprint or project.
- [ ] 2.3 Limit Grit to positive local declaration, composition, and internal import forms; assign filesystem closure to Habitat, cross-project/package edges to Nx, internal module direction to focused Grit plus owner-local fixtures, capability visibility to TypeScript, and runtime uniqueness/lifecycle to behavior tests.
- [ ] 2.4 Record the existing repository scheduler as the one Nx contract for the public SDK, ten private runtime owners, tools, and app projects: one workspace-owned cacheable `habitat:lint`; cacheable owner `typecheck`, `test`, and `build`; optional qualified `verify` and generated `check:policy`; and non-cacheable no-op `check` depending on shared lint, owner typecheck/verify/policy, and `^check`. Keep `test` and `build` as explicit `ci` graph targets rather than hiding them in `check`. Define complete cache inputs, including `.editorconfig` and every command-read tool/config file, plus explicit SDK/app manifest and `acceptance:<capability>` contracts. Create or normalize no project and run no target in this law-only task; each owner's first implementation owns project creation, exact edges, target realization, cache-hit proof, and one relevant-input invalidation proof.
- [ ] 2.5 Extend the global AGENTS router law to every admitted runtime owner with purpose, boundary, flow, interfaces, and ownership rather than code narration; retain `tools/workstream-plugin-pack` as qualified non-public Habitat tooling and remove stale Rawr-owned-generic wording.
- [x] 2.6 Apply the completed semantic sieve in [[classification-ledger]] to every current app, service, resource/provider, plugin, CLI command, package, tool, policy, test, and product record. Classify each as Habitat platform, proven Rawr product, qualified conformance fixture, or delete from behavior and invariant evidence rather than its current name. Record one exact destination and acceptance owner for every retained capability; ambiguity results in deletion.
- [x] 2.7 Select the existing independent Rawr repository `/Users/mateicanavra/Documents/.nosync/DEV/habitat/rawr` (`rawr-ai/rawr`, canonical `origin/main` `b02a9394a1476a90c11a871b678e28591d69bfa3`) through a clean worktree and author its owner-local OpenSpec. Treat the dirty primary checkout as read-only and excluded; do not edit, clean, or derive deletion authority from its user changes. Disposition the tracked Turborepo-era baseline, establish the destination Nx consumer shape, and record the initial finite migration ledger for the six admitted service/topic projects plus the Session Metrics lineage. Record later workstream and research adoption sources separately without admitting or importing them at the initial separation gate. Land this planning/scaffold checkpoint without importing product source or selecting commands.
- [x] 2.8 Land the foundational Habitat service constitution and its first complete repository descent as small truthful Graphite nodes, then cross the command publication barrier through exactly Gates A, B, and C below. Before Gate A, delete predecessor reader closures already classified for deletion and requiring no retained-capability transfer. This semantic sieve neither revives condemned code nor substitutes for the publication barrier around surviving readers. The barrier exists only to order producer publication before reader cutover; it is not a retained compatibility architecture.
  **Gate A - foundation producer.** Define the settled generic service kind as
  selected, closed, and natively constructible `service@1` law in the SDK
  Habitat pack; it is not an interface prerequisite, compatibility packet, or
  downstream integration aid. Create the private package-less `runtime-schema`
  owner at `packages/core/runtime/schema` with zero private dependencies, the
  exact public assembly edge `@habitat-ai/sdk -> runtime-schema`, and the task
  2.4 scheduler contract; realize its `typecheck`, `test`, `build`, and `check`
  targets and prove one unchanged cache hit plus one relevant-input
  invalidation. Move the one TypeBox-native adaptation there, prove its exact
  ambiguous-path behavior through `runtime-schema:test`, and expose it through
  `@habitat-ai/sdk/service/schema`. Move service metadata,
  analytics/observability middleware contracts, and host-neutral client
  contracts to `@habitat-ai/sdk/service`, add the SDK's standard `test` target
  for the exact metadata/context behavior in
  [[classification-ledger#behavioral-acceptance-matrix]], and expose the one
  `HabitatCommand` result/error/output contract through `@habitat-ai/cli` after
  its standard `test` target passes. The repository root MUST retain registry
  `@habitat-ai/cli@0.5.2` as its Nx bootstrap, `apps/habitat` MUST remain outside
  the Bun workspaces, and Bun 1.3.14 MUST retain a valid frozen lock. Do not
  introduce any blueprint or instance schema change, component DTO, parser
  branch, structure merge, or target-collapse behavior that requires the
  unpublished candidate plugin to project Gate A; every service packet MUST
  remain an ordinary schema-version-1 rule that the `0.5.2` bootstrap can
  project, and exactly one root Habitat structure rule MUST own the complete
  positive service filesystem closure. Do not source-link the candidate CLI
  through `file:`, `link:`, a second workspace
  identity, or any equivalent local dependency. The remaining private
  `RawrCommand`/`RawrResult` source and readers remain unchanged in the
  predecessor owner during this gate; Gate A revives no condemned closure and
  adds no reader, export, shim, alias, or fallback. Before any tag or
  public-registry mutation, run
  `nx run @habitat-ai/cli:acceptance:oclif-installed-package` against the exact
  candidate: it MUST pack both public tarballs, publish both exact candidates
  only to its isolated local registry, start a disposable Bun/Nx consumer with
  neither Habitat product installed, invoke native
  `nx add @habitat-ai/cli@<candidate-version>`, and prove that this one operation
  installs the paired SDK, converges the repository foundation, constructs
  `service@1`, loads the installed CLI/plugin, preserves exact dependency
  closure and cold imports, and exposes exactly one public/candidate command
  model, `HabitatCommand`. Gate A MUST NOT claim predecessor-model or reader
  absence.
  Land this accepted producer on exact Habitat `main`.
  **Gate B - exact-main publication.** From the accepted Gate A exact-main
  revision, use only the existing fixed Nx Release group to publish
  `@habitat-ai/sdk` and `@habitat-ai/cli`, registry-install-smoke those exact two
  artifacts, and record the exact version, main commit, package integrity,
  policy-pack digest, and acceptance receipt. Publish no service, private
  runtime, product package, or third artifact.
  **Gate C - reader cutover and predecessor deletion.** Only after the Gate B
  registry receipt exists, replace the root Nx bootstrap with that exact
  registry CLI version, migrate the admitted six-project Rawr readers and
  retained Habitat readers to the released `HabitatCommand` contract, delete
  any condemned reader closures still remaining rather than adapting them, and delete
  `RawrCommand`, `RawrResult`, their exports, source, and every remaining reader
  in the same green node. Gate C MUST end with zero predecessor command model or
  reader and MUST retain no shim, alias, fallback, or dual public authority.
  The six transfer projects do not depend on `findWorkspaceRoot`; task 2.11
  freezes and deletes the predecessor discovery plus every remaining reader,
  while later fresh command owners receive explicit workspace input at their
  own realization tasks. Task 2.10 moves none of this discovery closure.
  `service@1` MUST
  require one public contract, in-process client, router, and private
  implementation; service-to-service edges MUST use public clients;
  resources/providers MUST enter at app composition; and
  transport/orchestration MUST remain plugin/app concerns. A non-Effect oRPC
  operation MUST use native `.handler`; an Effect-backed operation MUST use the
  selected official `@orpc/experimental-effect` `.effect` extension installed
  once in `src/service/impl.ts`. Gate A MUST reject service-authored direct `Effect.run*`, a manual Promise bridge, a
  custom Effect runner, or `ProcessExecutionRuntime` as the executor of an oRPC
  service Effect. Delete each
  non-command predecessor export or standalone adapter in the same node as its
  final replacement, select `service@1` in the shipped pack, and expose no Oclif
  host seam before task 11.4, live runtime binding, app selection, provider
  acquisition, compatibility alias, product policy, or embedded placeholder
  adapter.
  Task 2.8 replaces the legacy v2 service packets atomically with the positive
  closed `service@1` authority from task 2.0. Root `structure.toml` remains one
  ordinary Habitat structure rule and owns the complete positive filesystem
  closure. Blueprint-owned context-bearing authoring directories group focused
  ordinary Grit assets only; each
  asset is referenced from the existing ordered `blueprint.rules` surface and
  retains its normal application and focused target. The definition and instance
  schemas remain unchanged and declare zero blueprint relations.
  Modules and optional database persistence are not child kinds. No monolithic
  source-law packet is required. Product-named rule metadata,
  generic `forbids` packets, and superseded service source-policy variants are
  deleted, not shipped beside the new kind. The installed-package acceptance
  MUST inspect every recursively packed service definition, root anchor,
  authoring directory, and rule-referenced asset for source-byte parity and
  refuse a missing nested asset, product vocabulary, or legacy rule packet.
  Selection MUST co-land with explicit `service@1`
  manifests for every service project still present in Habitat and with the
  repository-wide service structure, declaration, composition, direction,
  type, and behavior checks green. A generated fixture or green predecessor
  packet cannot substitute for this native descent. A project that is not a
  valid service MUST transfer or disappear before selection rather than remain
  as an ungoverned predecessor. Gate B records one exact
  version/main-commit/policy-pack-digest/acceptance receipt; Gate C consumes it,
  and only after Gate C closes may that handoff go to Civ7 and every waiting
  consumer.
- [x] 2.9 After task 2.8 Gate C has closed, in the clean Rawr destination run only `bunx nx add @habitat-ai/cli@<released-version> --no-interactive` against the exact Gate B receipt; record the initializer-installed exact `@habitat-ai/sdk` dependency and complete repository foundation rather than hand-authoring Habitat wiring. Then use the recorded native `nx import` invocations to import the ChatGPT corpus, Hyperresearch, and session-intelligence service/topic projects from one frozen Habitat source branch into Rawr with filtered Git history. First restack the Session Metrics lineage onto the admitted session service and include its accepted product behavior in the same owner transfer. Import service leaves before topic dependents, move topics to `plugins/cli/topics/<topic>`, remove synthetic Hyperresearch production fixtures, retarget every edge to the released interfaces, pass the Rawr Nx graph and named behavior targets from [[classification-ledger#behavioral-acceptance-matrix]], and land Rawr canonical `main`. Create no custom copier, mirror, sync, compatibility app, or continuing ancestry dependency. Rawr migrated to exact `@habitat-ai/cli` / `@habitat-ai/sdk` `0.5.10`, landed the six native imports through PR #57 at `7bce8bd1069b0d8c1c92fedb6f6ca01a633c5ac6`, and sealed the Hyperresearch production-fixture retirement through PR #59 at canonical `main@a1a4fe7ed051ff405605c82c09ccd73332595383`.
- [x] 2.10 In Habitat, qualify only the existing pre-runtime Habitat owners and
  their current readers without adding runtime or installed-vertical behavior:
  `services/catalog` / `@habitat-ai/catalog-service`; the provider-neutral
  agent-plugin package-output, content-workspace, native-agent-provider, and
  versioned-content resource families plus their existing provider projects;
  and `services/agent-plugin-lifecycle` /
  `@habitat-ai/agent-plugin-lifecycle-service` plus its public-client readers
  and owner-local tests. Preserve the already-selected `service@1` descent,
  move service dependencies only through public clients, pass each resource,
  provider, catalog, and lifecycle owner-local contract/conformance target, and
  remove each superseded Rawr-prefixed owner identity and reader in its own
  accepted node. This task does not create an agent-plugin CLI topic or overlay,
  select an app profile or provider, prove an installed command vertical, move
  the development vertical, move generators, activate final Oclif-app or
  CLI-topic law, or define the private CLI source-bundle contract.
- [x] 2.10a Before task 2.11, transfer the bounded native Oclif plugin owner from
  `apps/cli` to `@habitat-ai/cli`: move the exact `@oclif/plugin-plugins`
  dependency, Oclif configuration/manifest composition, and installed
  root `plugins` listing operation plus `install`, `link`, `inspect`, `update`,
  `reset`, and `uninstall` roundtrip. Co-land and record the
  `@habitat-ai/cli:acceptance:oclif-native-plugins` receipt against packed,
  installed CLI artifacts and disposable extension/state homes. Keep its
  indispensable prebuilt package fixture owner-local to `@habitat-ai/cli` test
  support, with no Nx project, workspace membership, source owner, command
  topic, application, or release membership. In the same node, delete the
  obsolete product-bound `.habitat/blueprints/oclif-app/**` packet and its three
  registered rule applications/catalog expectations; prove no replacement
  Oclif-app or topic law is active. The active
  `external-cli-extension-boundary` modification delta is the governing command
  authority for this cutover; do not hand-edit or delete its canonical main spec
  before task 15.9 applies the complete change. Update `AGENTS.md`,
  `AGENTS_SPLIT.md`, `README.md`, `CONTRIBUTING.md`, the `apps`/`apps/cli`/
  `plugins` routers, and the active product/process/cross-repository/HQ usage,
  operations, and runbook gateways to state exact current truth: external
  extensions use `habitat plugins`, while curated lifecycle remains the
  unmigrated `rawr agent plugins` channel until task 2.11 deletes that source.
  Rewrite no archive or quarantine history. This transfer
  adds no agent-plugin topic or profile, app/provider selection, runtime owner,
  final Oclif-app or CLI-topic law, or private CLI source-bundle contract.
- [x] 2.10b Before task 2.11 freezes the pre-deletion tree, adopt the latest
  exact-main bounded foundation-continuation release inside the sealed first
  functional checkpoint.
  Require `habitat-cli-v0.5.14` at
  `9e650107ac8c292ca13be5edc2390b870b952c68` and Publish Habitat run
  `31311235419`; migrate the root to exact registry
  `@habitat-ai/cli@0.5.14`, retain the producer SDK as `workspace:*`, lock the
  CLI's exact paired registry `@habitat-ai/sdk@0.5.14`, and apply no source
  link, fallback, or package-local migration because the release declares none.
  Change only `blueprintVersion` in retained instance manifests, preserving
  `schemaVersion`, id, `ownerProject`, roots, and selections: select
  `service@2` only for the retained catalog and
  agent-plugin-lifecycle services, and select `resource@2` only for the retained
  source-inventory, rule-evaluation, agent-plugin-package-output,
  content-workspace, native-agent-provider, and versioned-content resources.
  Keep provider selections at `provider@1`; leave the four transferred/deleted
  service instances and deleted agent-plugin-export-destination resource at
  version 1 until task 2.11 removes them. Prove the frozen lock, exact-version
  catalog/Nx projection, bounded nonparallel policy execution, and all eight
  retained owner policy checks without app/profile provider selection, runtime
  realization, compatibility, or product behavior changes. Record the exact
  package integrity, provenance, exact nine-member policy-pack inventory,
  policy-pack digest, and installed acceptance in
  [[foundation-continuation-release-receipt]]. After the source adoption lands,
  record its exact-main commit/tree and these named local checks in
  [[foundation-continuation-adoption-receipt]] before checking this task.
- [x] 2.11 Only after the task 2.10a installed receipt and the task 2.10b
  [[foundation-continuation-adoption-receipt]] is sealed, freeze
  `main@cc494354449465fa4178f36d4d5222b4d4072f5d` with whole-tree identity
  `2eacd194803e542a579b9c6c845605123fcb2bbb` as the exact source immediately
  before deletion and as later owner-local test provenance, then land the
  Habitat deletion boundary. Rename the root package and Nx project exactly to the private
  `habitat-workspace`, update every root-identity reader, and delete `apps/cli`
  and its remaining manifest/source; the imported Rawr service/topic copies;
  the predecessor `services/dev`, `packages/dev-node`, and
  `plugins/cli/commands/devops` roots/readers while preserving only their frozen
  source evidence for task 12.2;
  the product-bound `.habitat/blueprints/oclif-command-plugin/**` packet and all
  of its registered rule applications/catalog expectations, leaving no
  replacement CLI-topic law until task 11.4;
  every other Rawr product document, superseded product-only capability, app root,
  package identity, and `.habitat/rawr` policy name, while retaining this active
  canonical change, its current removal deltas, and the canonical removal
  targets `openspec/specs/{rawr-cli-application,legacy-membership-retirement,mixed-plugin-lifecycle-retirement}/spec.md`
  until task 15.9 applies and archives them. Relocate only already archived or quarantined members of
  `docs/projects/rawr-final-architecture-migration/**` beneath
  `docs/projects/_archive/rawr-final-architecture-migration/**`, delete every
  active member plus the product-only HQ/design/coordination guides, example-todo
  project packet, reactive-codebase essay, and Rawr coordination spike named in
  [[classification-ledger#behavioral-acceptance-matrix]], and update every
  retained Habitat gateway/index, workstream reference, and Semantica default
  reader so no active link or executable default resolves into deleted product
  documentation. Retain every canonical agent-plugin/external/authoring spec
  named by this change's modification deltas; those active deltas govern the
  target during implementation and update main specs only at task 15.9. After
  deletion, current Habitat guidance MUST state that `habitat plugins` is
  operational and the curated CLI projection is absent until task 12.1; it MUST
  NOT advertise a Rawr alias or a premature `habitat agent plugins` route; the live
  `runtime-realization-type-env` tool/project after preserving only its frozen
  Git commit as provenance; the unused agent-plugin export-destination
  resource/provider; the remaining `packages/hq-sdk`,
  `packages/runtime-context`, and `packages/bootgraph` predecessor source after
  task 2.8 has moved its admitted interfaces; predecessor `findWorkspaceRoot`
  and product-named Habitat discovery plus every doomed reader; synthetic
  `codex-slice` and `run-fixture` production commands/projects; and every
  remaining dead, mixed, duplicate, or unclassified reader. The cumulative
  absence gate MUST also prove the pre-Gate-A semantic-sieve deletions remain
  absent, including predecessor HQ/server/example projects and their commands,
  state, and readers; it MUST NOT re-stage or revive them as task-2.11 work.
  Execute this gate through the fixed `@habitat-ai/cli` /
  `@habitat-ai/sdk` `0.5.14` pair published from
  `habitat-cli-v0.5.14` at exact
  `main@9e650107ac8c292ca13be5edc2390b870b952c68`; Publish Habitat run
  `31311235419` passed both installed-package platforms, npm OIDC
  publication, provenance, and registry-installed smoke. This released runtime
  serializes every inferred Habitat policy target and excludes generated,
  dependency, environment, and evidence directories before Grit acquisition.
  Co-land `habitat:acceptance:product-separation-absence` with the exact
  cumulative task-2.11 predecessor inventory from
  [[classification-ledger#deletion-and-replacement-oracles]] and run it together
  with `bun run check` before landing. At this checkpoint, the temporary source
  overlay owns source-spelled, unescaped static ES-style specifiers while
  project-owned TypeScript checks own executable module resolution. Task 3.3
  retires that repository-wide predecessor-name blacklist with the final mixed
  package and leaves the permanent gate composed from project TypeScript plus
  native Nx graph, package/lock/tsconfig, Oclif-manifest, and filesystem
  observations. This
  deletion and its frozen source oracles do not realize or accept
  the final agent-plugin topic/overlay/profile, development topic, Habitat
  generators, runtime, private CLI source-bundle contract, or final generic
  Oclif laws; tasks 11.4, 11.6, and 12.1 through 12.3 own those fresh
  realizations. Later owners may consult frozen source only as provenance or
  port admitted algorithms/tests beside the behavior they verify. Retain no
  `rawr-hq-template` root identity, `apps/rawr`, source path, Git
  synchronization, alias, fallback, duplicate executable owner, or production
  example project.
- [x] 2.12 Pass the Rawr exact-main repository gate and land the Habitat product-separation boundary before task 3 opens. Require Rawr's initial migrated source to contain only the surviving ChatGPT corpus, Hyperresearch, and session-intelligence product closures under its owner-local OpenSpec; require Habitat's root package and Nx project to be exactly the private `habitat-workspace`, contain no downstream product source or app and no live `runtime-realization-type-env` project; run `nx run habitat:acceptance:product-separation-absence` from Habitat exact main; and verify Marketplace remains an independent content repository. Later stack-only Rawr product adoption is independent post-separation work and cannot weaken or delay this gate. Tasks 3.1 through 3.4 remain cleanup and core reservation only; other than the bounded non-live `runtime-schema` adaptation from task 2.8, no private runtime implementation begins until task 3.4 passes Habitat exact-main with all Rawr-named, dead, and mixed predecessors absent. Sealed from clean canonical worktrees: Habitat `main@3e6341df406d0476b1e486f6e4b1102d7debc37c` / tree `06b6da8dfdd26a6502452efe53fc093f78e582a0` passed the uncached cumulative absence target and exact-main Repository Ratchet run `31322248337`; Rawr `main@a1a4fe7ed051ff405605c82c09ccd73332595383` / tree `daaf31ab65677a430c0fd433d1e9a73784316aaf` passed the repository check plus the three service test and three topic manifest/test closures; Marketplace `main@851a5b87e86278757eb99b952281b90a35e74869` / tree `140ae3777621d7fe20d217aaa84d6d800474facb` passed its repository check and exact-main content-ratchet run `31078317256` with exactly eleven content projects plus its repository owner.
- [ ] 2.13 Keep new packets non-enforcing until their first conforming owner is ready; co-land each closed enabled law with that owner and admit no red baseline, exception, or open scope.
- [ ] 2.14 Co-land every TypeBox structural schema with the semantic owner whose data crosses a boundary; keep `runtime-schema` limited to canonical schema adaptation, and create no ownerless service-wide or runtime-wide schema directory ahead of its owners.

## 3. Establish The Canonical Core Owners And Sole SDK Package

Tasks 3.1 through 3.4 are platform cleanup and core-reservation slices after
repository separation. Each retained capability lands with its reader and its
predecessor is deleted in the same green Graphite node. Absent SDK families do
not land as shells; each new public or private family lands later with its first
conforming implementation. The bounded `runtime-schema` prerequisite from task
2.8 is the only private runtime owner admitted before separation; it performs no
selection, acquisition, execution, mounting, or observation. Task 3.3 is the
sole atomic SDK path transfer; task 3.4 closes the platform-only gate before
every other private runtime owner. Task 3.4 is dependency cleanup and negative
artifact proof only: it creates no public face, private runtime owner, native
loader, host implementation, or blueprint.

- [x] 3.1 Re-author the path-qualified provider-neutral telemetry resource, OpenTelemetry Node provider, provider conformance, and core-singleton retirement from the held mixed telemetry source into fresh destination-owned Habitat nodes before core reservation. Move every surviving telemetry reader, delete the mixed-core telemetry export and signal hooks, and leave backend activation and Habitat profile selection to the exact runtime/app owners, with app-selected Oclif proof required no later than task 12.1. Treat this integration as runtime provisioning substrate: service, plugin, and command authors do not acquire a provider, configure exporters, or supply baseline instrumentation. Later runtime and harness owners attach foundational observations exactly once per selected process lease, while business logic may add semantic enrichment only; their acceptance fixtures must pass with zero author-authored telemetry bootstrap. Do not merge or restack the predecessor telemetry root, copy its obsolete host wiring, or duplicate the provider; retire that source only after destination acceptance. Downstream products select only the later released interface from their own repositories.
- [x] 3.2 Classify every current `@habitat-ai/sdk` root export, move an existing export only when its qualified destination already has a conforming implementation, and otherwise preserve that export at the root until its owning later task replaces it; do not create or publish absent `app`, `effect`, `execution`, `service`, `plugins/*`, or `runtime/*` families as empty shells. The exhaustive classification preserves the type-only `HabitatClient` and runtime `createHabitatClientForWorkspace` root exports for their current foundational CLI/Nx readers; task 11.5 owns their reader migration and replacement, and no SDK source, package export, empty family, or runtime owner changes in this node.
- [x] 3.2a Before task 3.3 relies on another Habitat policy pass, replace version-2 compatibility Grit's per-coverage filesystem globbing with one request-local Git-visible source-inventory observation shared by every selected application. Preserve exact acquisition-root containment, deterministic order, symbolic-link/non-file refusal, empty not-applicable behavior, and the existing version-3 root-pattern bounds; add no second runner, parser, ignore blacklist, compatibility-specific subject cap, or timing retry. Prove one observation, zero compatibility glob calls, ignored filesystem-present exclusion, tracked-non-file pruning, and installed-candidate behavior. From the accepted exact-main correction, publish and registry-smoke only `@habitat-ai/cli` and `@habitat-ai/sdk` `0.5.15` as a bounded foundation continuation. Explicitly inventory the already-qualified declarative `@habitat-ai/sdk/telemetry` face and prove it exports no provider acquisition, lease, exporter factory, instrumentation bootstrap, app/profile selection, or runtime mount; the private telemetry resource/provider remain outside release membership. Record the exact release in [[foundation-continuation-0-5-15-release-receipt]], adopt exact registry CLI `0.5.15` plus its paired registry SDK while retaining the producer SDK as `workspace:*`, and record [[foundation-continuation-0-5-15-adoption-receipt]] before checking this task or resuming task 3.3.
- [x] 3.3 After tasks 3.1, 3.2, and 3.2a leave no mixed-core reader or writer and the root runs the corrected exact registry substrate, atomically delete the `@habitat-ai/rawr-core` package/project identity, preserve `packages/core` as a Habitat-only namespace, move `packages/habitat-sdk` wholly to `packages/core/sdk` without changing `@habitat-ai/sdk`, replace `packages/core/AGENTS.md` with the Habitat-core router, and update every SDK reader, Nx edge, package path, release input, and repository metadata. Extend the cumulative `habitat:acceptance:product-separation-absence` inventory with the mixed-core identity and every removed reader and run it in the same node. Preserve no Rawr declaration, predecessor path, workspace dependency, alias, or compatibility import; land and pass the exact-main Habitat platform-only gate before task 3.4 opens. Sealed from exact Habitat `main@d29177af06810b9de905ed133d2dad9fab8abe29` / tree `c83b7aecf6bfe06630dbe0fcc17dff4c551e9198`: PR #932 passed Repository Ratchet and both installed-package platforms, and push-to-main Repository Ratchet run `31339103432` / job `93309813544` passed the full repository graph.
- [x] 3.4 Remove the ownerless root `elysia` and `inngest` dependency declarations, reconcile the lockfile, and close this node with negative artifact proof only. Add no `@habitat-ai/sdk` peer or optional metadata, export, subpath, loader, private runtime project, native host implementation, app or harness blueprint, or selection. Prove the current packed SDK and CLI public artifacts remain cold and isolated when Elysia and Inngest are unavailable; their packed manifests MUST contain no Elysia/Inngest dependency, peer, optional declaration, export, load path, or unpublished workspace dependency. Exercise rejected invalid and surplus structure through the existing real TypeBox runtime-schema and OpenTelemetry Node config decoders; add no schema walker, package walker, or whole-plan snapshot. Optional peer metadata and an owner-local conditional dynamic import MUST co-land with the first conforming host owner: Elysia in `runtime-harnesses` task 13.1, and Inngest in the `runtime-harnesses` task 13.3/13.4 sequence. Task 4.2's async face remains cold and host-neutral. Oclif remains a direct `@habitat-ai/cli` dependency and owner, not an SDK peer; OpenTelemetry, Effect, and oRPC remain their current substrate or mechanism dependencies. Pass the exact-main gate with every Rawr-named, dead, and mixed predecessor absent before task 4 opens. Sealed from exact Habitat `main@c59cb3255ac024e34ff0dc0c3aa01aa4572e5bcd` / tree `dc18105ac2a87cd59ede9c2143a66b5225bb8a90`: PR #934 passed Repository Ratchet and both installed-package platforms, and push-to-main Repository Ratchet run `31342319663` / job `93318013787` passed the full repository graph.

## 4. Seal Definition And Derivation

- [x] 4.1 Create the private package-less Nx project `runtime-definition` at `packages/core/runtime/definition`; inherit the exact task 2.4 scheduler contract, set its sole direct private edge to `runtime-definition -> runtime-schema`, add `@habitat-ai/sdk -> runtime-definition`, and prove unchanged cache hits plus relevant-input invalidation. In that same node, implement import-safe `AppDefinition`, finite cold `ProcessCatalog`, `Entrypoint`, `RuntimeProfile`, immutable `RuntimeLaunchIdentity` with exactly `{ app, process, entrypoint, deployment, source }`, service, plugin, and cold Effect descriptor authoring; direct provider-neutral resource and provider-owned config faces; the TypeBox `RuntimeObservationRecord`; and the narrow non-authorizing `RuntimeObservationPort` consumed by upstream phases and implemented downstream by runtime observation. Keep live `startApp(...)`, deployment placement, supervision, sibling control, and read-model projection out of definition. Expose only conforming app, Effect, execution, service, plugin, resource, provider, profile, and schema subpaths through the terminal SDK and co-activate their closed SDK/resource/provider law. The catalog and identity are app-owned cold data, not a `process` kind, child project, live registry, supervisor, deployment unit, or whole-app controller. Sealed from exact Habitat `main@2c0aa807a1d3a2c8b2e843be92e2088170e4fbba` / tree `a08884381c2e50cf58c7f4f51398f9eb016165c9`: PR #942's restacked head `edfb88a638fb62922dbba0773c2417aa004eebb3` passed Repository Ratchet run `31358179882` / job `93361686831` and installed-package acceptance on Ubuntu and Windows in run `31358179876`; `runtime-definition:test` proved the cold owner plus unchanged-cache restoration and relevant project/root-input invalidation; the installed acceptance selected `runtime-definition-acceptance`, evaluated `runtime_definition_v1_structure`, exercised the task-4.1 SDK subpaths, and preserved byte-identical `app@1`; and push-to-main Repository Ratchet run `31358233213` / job `93361832747` passed the full repository graph.
- [x] 4.2 Author the accepted topology-specific server/internal plugin face and host-neutral async plugin face fresh in the terminal `@habitat-ai/sdk/plugins/server` and `@habitat-ai/sdk/plugins/async` subpaths. Use installed native oRPC, official Effect-oRPC, and Effect as mechanism authority; native Inngest 4.18 source is future vendor evidence only and MUST NOT become an installed task-4.2 dependency. A synchronous or Promise-returning service operation MUST use inline native `.handler`; an Effect-backed operation MUST use the official implementation-owned `@orpc/experimental-effect@2.0.0-beta.23` `.effect`, installed once in `src/service/impl.ts` through the side-effect-only `@habitat-ai/sdk/plugins/server/effect` bootstrap. Co-land the complete `service@3` terminal-SDK-consumer successor, keep the published `service@1` and `service@2` bytes immutable, retain SDK-internal services on an acyclic earlier selection, and enforce facade placement plus direct-vendor/manual-runner absence through selected Habitat source law rather than source-string tests. The extension's internal `handlerGen` delegates to native `.handler` and owns `Effect.runPromiseExit`; Habitat-authored authoring, adapter, and operation code MUST NOT directly import, call, wrap, or reimplement `handlerGen`, while the selected official extension's internal call remains admitted. The async face remains cold and host-neutral: it MAY emit only async declarations plus stable identity and descriptor references for task 13 lowering. It MUST NOT create a `FunctionBundle`, registration factory, native client, native function, loader, adapter, harness, dispatcher materialization/descriptor without a named consumer, native-host peer or optional metadata, runtime-load Inngest, or pre-empt the task 13 native owner. Keep the server spine capable of carrying a future MCP surface projection, but add no MCP authoring face, SDK subpath, protocol implementation, role, kind, or lifecycle owner now. Reuse only independently admitted generic merge or trace-forwarding algorithms. Do not preserve the predecessor self-classifying `defineApiPlugin*` / `composeApiPlugins` builders, Inngest-shaped `defineWorkflowPlugin*` / `composeWorkflowPlugins` builders, names, or wire authority. Consume the already-landed `@habitat-ai/sdk/service` metadata and middleware contracts without redefining them, and admit no concrete placeholder adapter or product procedure policy. Sealed from exact Habitat `main@42c65376babeacae3058f865e1c680631879fc96` / tree `fd151f1baa628d714fa61cd6f696d3683d0c4e55`: PR #944's final head `af2ef17e8f061e10688cc74f01783203af2cbe19` passed Repository Ratchet run `31375226865` / job `93412840818` and installed-package acceptance on Ubuntu and Windows in run `31375226864`; the cold server/internal lanes, host-neutral async declarations, exact SDK faces, and selected source law passed their owner tests; `@habitat-ai/agent-plugin-lifecycle-service:check:policy` proved the complete `service@3` terminal-SDK-consumer successor while Catalog remained on acyclic `service@2`; installed acceptance exercised native `.handler` plus the official `.effect` bridge through one oRPC/Effect module realm, all four task-4.2 SDK subpaths, and byte-identical `service@1` / `service@2`; and push-to-main Repository Ratchet run `31376301029` / job `93416194667` passed the full repository graph. No Inngest/Elysia host dependency or metadata, native client/function, `FunctionBundle`, loader, adapter, harness, dispatcher materialization, MCP face, `app@2`, or live runtime owner landed early.
- [x] 4.3 Implement the sole cold service-use relation in `runtime-definition` and the terminal SDK service face. `useService(serviceDefinition, { contract, instance? })` MUST return frozen `ServiceUse<TContract>` with public enumerable `kind: "service.use"`, the exact definition `serviceId`, and optional `serviceInstance` only for a genuine composition-selected instance. Treat the containing services-map key only as the local injected-client property, never as alias, service/binding identity, or cache-key input; expose no public `service`, `contract`, or `alias`. Retain the exact definition and contract witness behind a `runtime-definition`-owned non-enumerable symbol carrier and internal accessor available only to private runtime owners and never re-exported by the SDK. Prove the exact public shape, omitted/explicit instance behavior, private recovery, `ServiceContractOf` inference, services-map key preservation, and identical `useService` re-export from the server and async SDK faces through owner tests, SDK TypeScript tests, and installed-package acceptance. Create no new kind, blueprint, blueprint version, project, or optional interior. Author no `ProcessView`, `RoleView`, `ServiceBoundary`, author-facing `ServiceBinding`, normalized/binding plan, five-lane context, live client, cache, host, or runtime surface: task 4.5 owns the five lanes, task 4.8 owns normalization and `ServiceBindingPlan`, and consolidated task 8.2 owns live binding/cache mechanics. Sealed from exact Habitat `main@da74c92df9164c67ca2b8622bb8f297f806802f9` / tree `b8c48d572839eef34e2dfa55239bde4cb720731b`: PR #947's final head `3bc7552b00fe3d228362b120b4e1be06ab728e93` passed Repository Ratchet run `31380563281` / job `93429569655` and installed-package acceptance on Ubuntu and Windows in run `31380563262` / jobs `93429569528` and `93429569473`; the runtime-definition owner test/typecheck/build, terminal SDK test/typecheck/build, installed consumer, and strict OpenSpec proofs passed; server API, server internal, workflow, schedule, and consumer declarations retained the exact frozen services map and contract inference; and push-to-main Repository Ratchet run `31381519180` / job `93432530311` passed the full repository graph. No normalized or compiled binding plan, five-lane context, live client, binding cache, host, provider selection, or runtime surface landed early.
- [x] 4.4 Audit and seal the post-task-4.3 platform-reader boundary without binding a production reader or selecting a provider. Prove analytics/logger doubles remain owner-local; preserve the landed task-4.2 SDK analytics/observability middleware behavior and agent-plugin lifecycle semantic events; and assert `FeedbackClient`, `DbPool`, `Sql`, their embedded adapters, predecessor generic host-adapter or test-helper package closures, and example-todo production projects remain absent. Task 4.8 owns cold provider selection, consolidated task 8.2 owns live reader binding and cutover, and tasks 11.3/12.1 own Habitat app/profile selection. A production analytics sink or a `Logger`-to-technical-telemetry adapter requires separate explicit authority and MUST NOT be invented here. Sealed from exact Habitat `main@85695b839869fb145a7eac0916cabdabd3805e39` / tree `3137b51a9e75e5e9dfb60b7f4788a94711a62914`: PR #949's final head `ef0fb2703e92783ebcbcd17cb2e540c7bec58646` passed Repository Ratchet run `31383137900` / job `93437528581`; the SDK and agent-plugin lifecycle typecheck/test targets, lifecycle policy, telemetry resource/provider owner tests, and cumulative product-separation acceptance passed; the structured Nx oracle proved exactly 24 retained projects at their canonical roots and the unique SDK project-dependency closure, with all seven absence cases and 37 assertions green; and push-to-main Repository Ratchet run `31383236907` / job `93437844317` passed the full repository graph. The only source change repaired that cumulative Nx oracle after task 4.1; no production source, reader binding, provider selection, analytics sink, telemetry adapter, resource/provider, project, package, kind, or blueprint changed.
- [x] 4.5 As the sole five-lane owner, author canonical `deps`, `scope`, `config`, `invocation`, and `provided` service context lanes fresh in `@habitat-ai/sdk/service` from the landed architecture law and prove module projection narrows them without overwriting; preserve no predecessor runtime-context package or workflow/support state. Leave the task-4.3 cold relation unchanged, derivation-owned binding references to task 4.8, and execution-facing contracts for task 8.3. Sealed from exact Habitat `main@9b9259b77f485e325d67c6cd726bcbb40932c207` / tree `1c58af9b52ada101101f8b3be397d54e03dea69e`: PR #951's exact head `c9b144979ec2770ae7cf8e1c3c783b3efe004d44` passed Repository Ratchet run `31385108500` / job `93443677318` and installed-package acceptance on Ubuntu and Windows in run `31385108488`; the SDK typecheck/test/build, installed consumer, cumulative product-separation target, and complete repository check proved the exact type-only five-lane boundary, readonly fields, valid projection preservation, and direct/optional/union/index-signature overwrite refusal; and push-to-main Repository Ratchet run `31386014522` / job `93446485490` passed the full platform graph. No runtime value export, service binding, execution context, provider selection, host, project, package, kind, blueprint, or dependency edge changed.
- [x] 4.6 Author the canonical route-projection and web-harness contracts fresh in `@habitat-ai/sdk/plugins/web` and prove the terminal SDK web subpath remains cold without selecting a browser runtime; preserve no predecessor `HTMLElement` mount protocol, package, alias, or product web composition. Sealed from exact Habitat `main@9c54fe541d8d093b5d0c99744f7ca4ae7bb5b29f` / tree `7bc21fddd24755b680e85fff294f55d4464c60a7`: PR #953's exact head `0442880bd762ee6850b3210a52763863741e383b` passed Repository Ratchet run `31389129468` / job `93456383411` and installed-package acceptance on Ubuntu and Windows in run `31389129448` / jobs `93456383284` and `93456383340`; the runtime-definition and terminal SDK type/public-face proofs plus installed consumer proved the frozen `web/app` definition, literal route types, preserved lazy route-module loader identity, serialization of only `{ id, path }` route facts, exact empty service/resource selection, and the isolated `@habitat-ai/sdk/plugins/web` face with zero router-vendor resolution; and push-to-main Repository Ratchet run `31390418177` / job `93460568063` passed the full platform graph. No browser runtime, web-selected router vendor, DOM/`HTMLElement` mount protocol, adapter, build execution, app composition, Effect subpath, native harness lifecycle, project, package, kind, blueprint, dependency edge, or new vendor dependency/peer/optional metadata landed early.
- [x] 4.7 Create the private package-less Nx project `runtime-derivation` at `packages/core/runtime/derivation`; inherit the exact task 2.4 scheduler contract, set its only direct private edges to `runtime-derivation -> runtime-schema` and `runtime-derivation -> runtime-definition` through real source/build references, and select immutable topology-only `runtime-derivation@1`. Keep the checked-in closure exact: root `AGENTS.md`, `habitat.toml`, `project.json`, `src/`, `test/`, `tsconfig.json`, `tsconfig.test.json`, and `tsdown.config.ts`; only `src/index.ts` plus `src/normalized-runtime-topology.ts`; and only `test/normalized-topology.test.ts` plus `test/nx-cache.test.ts`. Implement only private `deriveNormalizedRuntimeTopology({ entrypoint, profileId })` and closed TypeBox `NormalizedRuntimeTopology`. Define exact plugin, surface, and effective resource-requirement identity shapes; recursively copy/freeze output; check selected identity; derive roles only from the process, plugin/surface/resource facts only from app plugins, and transitive service edges only through sealed `ServiceUse` carriers; admit no `plugin.service`. Use only the exact typed edge variants and dependent-to-dependency service direction, treating self-loops as cycles. Refuse duplicate plugin identities, role literals, surface tuples, exact edge tuples, and every service cycle. For self-loops and longer cycles, acceptance proves only order-independent refusal; it does not prescribe an error class, selected cycle path, diagnostic ordering, or finding payload and adds no error API. Make `resourceRequirementIdentities` the deterministically sorted unique projection of accepted `plugin.resource` edge resources: admit one shared resource demand across distinct plugin identities but reject a duplicate exact `plugin.resource` edge. Sort complete tuples by ECMAScript code-unit order with missing optionals compared as empty. Co-land `runtime-derivation:acceptance:normalized-topology` proving closed-schema rejection, deep-equal/nonidentical identity, recursive freeze, order equivalence, exact refusal semantics, opaque deployment/source copying, zero cold-body calls, and cache hit/invalidation. Add no optional interior, `package.json`, complete artifact, SDK edge/export, or later blueprint behavior; version 1 remains immutable. Sealed from exact Habitat `main@8e11ac0140a31bcdda3686cb9927e1d331379c8d` / tree `957a437af278ce431cb5343b080ba6e2752fd3ea`: PR #956's final head `e89792a7f0b9a56905152ce6b011cc9a63fce0d9` passed Repository Ratchet run `31409104862` / job `93522581950` and installed-package acceptance on Ubuntu and Windows in run `31409104878` / jobs `93522581691` and `93522581758`; the runtime-derivation owner typecheck, test, build, selected `runtime_derivation_v1_structure` policy, normalized-topology proof, and cache hit/invalidation proof passed; the exact package-less root, immutable topology-only `runtime-derivation@1`, two real private source/build edges, fresh recursively frozen topology, deterministic ordering, exact identity/duplicate/cycle/schema refusal, shared-resource projection, opaque identity copying, and zero cold-body calls were proved; and push-to-main Repository Ratchet run `31410430940` / job `93526897423` passed the full platform graph. No `package.json`, SDK edge/export, complete graph, descriptor/module table, portable plan, provider acquisition, execution, lifecycle, public error API, optional interior, or later blueprint behavior landed early.
- [x] 4.7a Freeze the complete-derivation and binding-source contract without implementation. Exactly eight documents carry the correction: `HABITAT_ARCHITECTURE.md` routes, `HABITAT_RUNTIME_REALIZATION.md` is the sole exact canonical document, and six active OpenSpec artifacts retain the sole archive-safe acceptance requirement/scenarios. Canonical §§11.8, 13.5, 15, 23.1, and 27 own task 4.8 mechanics. This authority correction changes no implementation, project, blueprint, SDK edge, export map, public export, stage, or commit; immutable `runtime-derivation@1` remains unchanged, and tasks 4.7b, 4.7c, 4.7d, and 4.7e MUST all complete before task 4.8 becomes the sole active source node.
- [x] 4.7b Correct the remaining execution-identity, async-step-lowering, and service-resource-normalization authority without implementation. Across the same exact eight-document authority surface, keep canonical `HABITAT_RUNTIME_REALIZATION` as the sole mechanics owner and the active `app-runtime-realization` requirement/scenarios as the sole archive-safe OpenSpec acceptance owner. Freeze `executionDescriptorId(...)` as `execution-descriptor:sha256:` plus 64 lowercase hexadecimal SHA-256 characters over UTF-8 RFC 8785 canonical JSON of `{ kind: "execution.descriptor-identity", ...the exact closed boundary-specific ExecutionDescriptorIdentityInput }`; require canonical `executionId` and `ownerId` patterns and descriptor/full-ref/recomputed-id and boundary agreement. Require complete derivation to lower every async-step occurrence under its full ref into a distinct frozen operational `execution.effect` descriptor with the exact authored frozen policy and effect-function reference. `run(...)` invokes no authored code before returning a cold `HabitatEffect`; only execution of that Effect invokes the authored function, with a generator normalized through definition-owned `Effect.gen`. Reuse under distinct workflow/schedule/consumer parents derives distinct descriptor ids, and `ExecutionDescriptorTable` carries the derived operational descriptors rather than authored async descriptors. Require each service `resourceDep` to normalize exactly to owner `{ kind: "service", serviceId, localName }`, resource id and `resource.defaultLifetime`, `role` if and only if that lifetime is `role`, no `instance`, `optional: false`, and `reason: localName`; retain authored reason for plugin/provider requirements. Add archive-safe proof/refusal scenarios for identity mismatch, lazy lowering and parent reuse, and process/role lifetime normalization. Preserve the exact task-4.8 corpora. Change no implementation or source, including `packages/core/runtime/definition/src/execution.ts`, and no project, blueprint, SDK edge, export map, public export, stage, or commit; immutable topology-only `runtime-derivation@1` remains unchanged. Only after tasks 4.7c, 4.7d, and 4.7e also complete is task 4.8 the sole active source node under independent `runtime-derivation@2`.

- [x] 4.7c Narrow task-4.8 execution-ref/table population and its public-entrypoint acceptance without implementation. Across the same exact eight-document authority surface, retain the exact five-boundary-variant `ExecutionDescriptorRef` and `ExecutionDescriptorTable` contracts for future-compatible later lane admission, but require task 4.8 to populate them only with operational descriptors derived from authored async-step occurrences. Every other variant remains conditional on a later separately admitted lane-owned authoring carrier and lowering; `plugin.web-surface` is schema vocabulary only now, not a task-4.8 entry or an early web Effect face. Replace the impossible non-async exact-result fixture with one authored async occurrence plus one lazy web loader and prove the derived async operational descriptor plus preserved loader through the actual public export. Arbitrary properties, project facts, casts, synthesized refs, and direct table fixtures cannot prove the public entrypoint or population. Preserve the exact task-4.8 corpora and change no implementation, source, test, project, blueprint, SDK edge, export map, public export, stage, or commit; immutable topology-only `runtime-derivation@1` remains unchanged. Only after tasks 4.7d and 4.7e also complete is task 4.8 the sole active source node under independent `runtime-derivation@2`.

- [x] 4.7d Admit the existing `packages/core/runtime/definition/AGENTS.md` router as the eighth task-4.8 behavior-companion file solely for ownership and routing documentation. This sealed documentation-only correction spans the prior eight authority documents plus that router, for exactly nine documents; it changes no implementation, source, test, project, blueprint, SDK edge, export map, public export, publication/assembly corpus, stage, or commit, and immutable topology-only `runtime-derivation@1` remains unchanged. Record that flat runtime-definition `profile.ts` owns cold object-shaped `providerSelection(...)` grammar, projected only through `@habitat-ai/sdk/runtime/profiles`, while provider Effect plans and acquisition remain later runtime responsibilities. Only after task 4.7e also completes is task 4.8 the sole active source node under independent `runtime-derivation@2`.

Canonical `HABITAT_RUNTIME_REALIZATION` §§11.8, 13.5, 15, 23.1, and 27 own task
4.8's exact mechanics. The active
`app-runtime-realization` requirement and scenarios own archive-safe
acceptance, including the task-4.7b identity, lazy-lowering/reuse, and service
process/role-lifetime scenarios plus the task-4.7c population and
public-entrypoint correction and the task-4.7d definition-router ownership
correction. Task 4.7e adds the crypto build-authority correction only. The task
list contains execution and proof routing only; it is not a duplicate semantic
authority.

- [x] 4.7e Correct task-4.8 crypto build authority without implementation. Across exactly the same eight authority documents used by tasks 4.7a-c, excluding `packages/core/runtime/definition/AGENTS.md`, add the existing `packages/core/runtime/derivation/tsdown.config.ts` to task 4.8's exact publication/assembly corpus. Permit task 4.8 to change that file only by adding `node:crypto` exactly once to `deps.onlyImport`, yielding exactly `["@orpc/contract", "@orpc/server", "@standard-schema/spec", "node:crypto", "typebox"]`, while retaining `platform: "neutral"`, every prior entry, and every other option. Record that synchronous RFC 8785/SHA-256 identity uses Node native `createHash` and that pinned tsdown 0.22.14's neutral-platform `onlyImport` audit otherwise rejects the emitted builtin. Forbid a platform change, hand-rolled digest, Bun-only crypto, async WebCrypto, new dependency/package/Nx/public/source-semantic change, or any config edit in this documentation-only task. Preserve the exact task-4.8 source/test and eight-file behavior-companion corpora, every other publication/assembly file, immutable topology-only `runtime-derivation@1`, all package/blueprint/directory counts, and all exports. Only after this completed gate is task 4.8 the sole active source node under independent `runtime-derivation@2`.
- [x] 4.8 Implement canonical `HABITAT_RUNTIME_REALIZATION` §§11.8, 13.5, 15, 23.1, and 27 plus the active `app-runtime-realization` acceptance requirement/scenarios under independent complete `runtime-derivation@2`, with no invention or widening. Create exactly `.habitat/blueprints/runtime-derivation/versions/2/{blueprint.toml,structure.toml}` and select version 2 in the existing derivation `habitat.toml`; do not edit, inherit from, or fall back to immutable topology-only version 1, and add no version 3. Retain the exact root/source/test closure, exact eight-file behavior companion corpus, and separate exact publication/assembly corpus named by the acceptance requirement, including the existing `packages/core/runtime/derivation/tsdown.config.ts`. Change that config only by adding `node:crypto` exactly once to `deps.onlyImport`, producing exactly `["@orpc/contract", "@orpc/server", "@standard-schema/spec", "node:crypto", "typebox"]` while retaining `platform: "neutral"`, every prior entry, and every other option; use Node native `createHash` for synchronous RFC 8785/SHA-256 identity. Do not change the platform, hand-roll a digest, use Bun-only crypto or async WebCrypto, add a dependency/package/Nx/public/source-semantic change, or alter another config option. Create no new `runtime-definition` file/project/blueprint/version or other kind/version/project. The publication proof MUST add the exact LF rule, grow the SDK policy pack from 11 to 13 sorted members with `runtime-derivation@1`/`@2`, grow copied/input blueprint directories from eight to nine, pin the version-1 root closure excluding `versions/` and the exact version-2 closure, prove packed parity/provenance and the public import, and actually apply version 2. Evolve only the private `ServiceUse` binding carrier in existing `service.ts`, add cold object-shaped `providerSelection(...)` to flat existing `profile.ts`, retain the already-corrected `AGENTS.md` router unchanged as the eighth routing companion, and export that helper only through `@habitat-ai/sdk/runtime/profiles`. Through sole `@habitat-ai/sdk/runtime/derivation`, expose the one synchronous operation, exactly three runtime values, finite type-only inventory, and no public error API. Return exactly `{ topology, graph, executionDescriptorTable, webRouteModuleTable, portableArtifact }` after one private topology call, preserve `graph.topology === topology`, recursively freeze every result and one-time table snapshot, and invoke no Effect body or web loader. Emit the exact singular-profile graph; normalize source defaults, authored order, expanded refs, the private binding tree, config-schema iff rules, recursive inheritance/override/diamond law, RFC 8785/SHA-256 identities, deduplicated binding plans, sole optional-provider finding, and built-in `TypeError` refusals. Prove source normalization and ref expansion with zero source I/O/decode plus binding-id/dedupe only; physical resolution waits for task 7.2 and cache construction waits for task 8.2. Keep Effect and web refs/tables disjoint, preserve exact descriptor/loader values behind eager full-structural-ref lookup, and emit/roundtrip only the exact seven-field portable artifact. Add the real SDK edge/export, installed-package acceptance, and `runtime-derivation:acceptance:deployment-cold-plan`; prove every archive-safe scenario in the acceptance requirement and add no optional interior.
  Task 4.8 populates execution refs, the execution table, and portable execution
  refs only from authored async-step occurrences. Its five-boundary-variant
  contracts remain future-compatible, while each non-async variant requires a
  later admitted lane-owned carrier and lowering. `plugin.web-surface` remains
  schema vocabulary only. Acceptance invokes the actual public SDK export with
  one async occurrence and one lazy web loader; it proves the derived async
  descriptor and preserved loader without arbitrary properties, project facts,
  casts, synthesized refs, or a direct table fixture.
- [x] 4.9 Audit frozen Runtime Realization Lab commit `3147acbdcdd916883cee5b081c0868e3d1bf09b9`, whole tree `7fff3eaf6d80a4609dd0d511696212a38133753d`, and `tools/runtime-realization-type-env` subtree `d35cd11d21abf6831947a57638cbd7de8035bf0d` against landed `runtime-derivation@2`; accept an authority-only no-op because task 4.8 already re-authored reference identity/agreement, service-binding deduplication, surface grouping, workflow inventory, async ownership/laziness, cold tables, and refs-only portability and no distinct derivation algorithm remains to port. Reject lab `stableJson`/`exec:*` identity, explicit binding inputs, mutable shapes, public types, Oracle, alternate `deriveRuntimeSpine`, and route derivation without an admitted carrier. Leave provider graph matching, closure, cycles, and built-in refusal proof to compiler tasks 5.2 and 5.4 without creating compiler diagnostics. Change no source, test, project, blueprint, SDK face, public contract, Oracle, optional interior, version 3, or alternate path; preserve `runtime-derivation@2` exactly.
- [x] 4.9a Correct definition-to-selection authority without implementation across exactly nine documents: `HABITAT_ARCHITECTURE.md`, `HABITAT_RUNTIME_REALIZATION.md`, `packages/core/runtime/definition/AGENTS.md`, and the six active OpenSpec artifacts. Establish `Entrypoint` as the sole cold selection artifact and require `defineEntrypoint(...)` to synchronously produce it from real `AppDefinition`, `RuntimeProfile`, and `ProcessDefinition` values, the entrypoint id, and exact five-field `RuntimeLaunchIdentity`. Before return or publication require `identity.app === app.id`, `identity.process === process.id`, and `identity.entrypoint === id`; mismatch throws built-in `TypeError` before output, external mutation, or authored executable invocation, without a public error API or prescribed text/order. Keep profile agreement in task 4.11 because launch identity has no profile field, and retain every derivation check defensively. Change no implementation, source, test, project, blueprint, SDK face, public contract, export, package, or runtime behavior; task 4.10 becomes the sole next implementation node.
- [x] 4.10 Implement the definition-to-selection producer check in exactly `packages/core/runtime/definition/src/app.ts` and `packages/core/runtime/definition/test/definition.test.ts`. Preserve existing signatures and inference, the result's exact app/profile/process references, freeze behavior, and SDK export identity. Use real constructors, make producer-local bindings unavailable after handoff, prove each of the three app/process/entrypoint identity mismatches throws built-in `TypeError` before output or external mutation, and prove zero authored executable work. Add no validator, schema, file, project, edge, blueprint, version, export, or error surface. Sealed from exact Habitat `main@6c5e6f70af89ff884d59d6df6d7ec20ac980fe19` / tree `4be43cbd7465a283c3357862af293c0d58682332`: PR #966's exact final head `7c7dec98cdb72fdfde690e2bb5f468b1e4c22782` changed only the authorized two-file corpus and passed Repository Ratchet run `31442645520` / job `93630372632`; push-to-main Repository Ratchet run `31442954471` / job `93631270608` passed the full repository graph. Task 4.11 remains unimplemented.
- [x] 4.11 Add the selection-to-derivation handoff proof only to `packages/core/runtime/derivation/test/complete-derivation.test.ts`. Use a real `Entrypoint` plus `profileId`, complete derivation with selection source unavailable after handoff, and independently corrupt the three app/process/entrypoint identity agreements and profile agreement; require built-in `TypeError` before a derivation result and zero Effect-body or loader invocation. Retain every derivation check defensively and change no derivation source or public surface; add no file or blueprint version and keep `runtime-derivation@2` final. Sealed from exact Habitat `main@63f97bec01c6c72b409d9a6de3b0379fd91ba1e1` / tree `9f28f4d9fef987c2bf8e330e5bd7a19c8222537e`: PR #968's exact final head `02a5f45572628045f0d5568ae16ae6687296579d` changed only the authorized derivation behavior-proof file and passed Repository Ratchet run `31444104830` / job `93634577099`; push-to-main Repository Ratchet run `31444366578` / job `93635322604` passed the full repository graph. Task 5.0 is now sealed below as the documentation-only compiler-authority correction, making task 5.1 the sole next source node.

## 5. Seal Compilation

- [x] 5.0 Correct runtime-compiler authority without implementation across exactly eight documents: `HABITAT_ARCHITECTURE.md` as router, `HABITAT_RUNTIME_REALIZATION.md` §16 as the sole exact mechanics and closed-TypeBox-DTO owner, and these six active OpenSpec artifacts with `app-runtime-realization` as the sole archive-safe acceptance owner. Freeze private package-less `runtime-compiler@1`, its exact final closure, sole synchronous `compileRuntimePlan({ entrypoint, graph }) -> { plan, references, observationSeed }` contract, built-in-`TypeError`-before-result refusal, cold reference-table and observation-seed boundaries, narrow lane/harness planning, task/proof allocation, and absence of a public face or early terminal-composition edge. Change no implementation, source, test, project, blueprint, SDK surface, public contract, export, package, runtime behavior, `.habitat` blueprint, or `.habitat` current-realization record. Copy no canonical schema block into the routing/record documents. Task 5.1 is the sole next source node.
- [x] 5.1 As the sole next source node, create and fully activate private package-less `runtime-compiler@1`. Create exactly `.habitat/blueprints/runtime-compiler/{blueprint.toml,structure.toml,skill.md}` and `packages/core/runtime/compiler/{AGENTS.md,habitat.toml,project.json,src,test,tsconfig.json,tsconfig.test.json,tsdown.config.ts}`; `src` MUST contain exactly `compile-runtime-plan.ts`, `compiled-process-plan.ts`, `index.ts`, and `runtime-compilation-reference-table.ts`, while `test` MUST contain exactly `compile-runtime-plan.test.ts`, `derivation-handoff.test.ts`, and `nx-cache.test.ts`. Establish only the real direct edges `runtime-compiler -> runtime-definition` and `runtime-compiler -> runtime-derivation`, never `implicitDependencies` or publication metadata. Implement the §16 closed TypeBox DTOs and synchronous baseline complete `compileRuntimePlan({ entrypoint, graph }) -> { plan, references, observationSeed }`: consume the exact `Entrypoint` and `NormalizedAuthoringGraph`; duplicate-check and canonicalize process roles; project the exact selected process roots and their transitive service/semantic/resource/provider/workflow/execution/web closure while excluding unrelated app-role facts; preserve the exact selected/unselected optional-resource branch law; recover cold provider/service definitions only through the `Entrypoint`; return `observationSeed` beside a plan containing neither `observationSeed` nor `findings`; and create no derivation-result/table/portable-artifact input. Co-activate and actually apply version 1, add the LF rule, grow the sorted SDK policy pack from 13 to 14 members and copied/input blueprint directories from 9 to 10, and prove exact structure, the two Nx edges, cache restoration/invalidation, packed parity, provenance, and application. Add no optional interior, `package.json`, `versions/` directory, Grit source law, successor blueprint, compiler package/public face/export, fake SDK export, or early terminal-composition edge. Task 10.6's later terminal SDK composition source MUST add the final real direct `@habitat-ai/sdk -> runtime-compiler` edge only when it imports and calls `compileRuntimePlan(...)`; runtime mounting gets no compiler edge and transitive process-runtime reachability is not a substitute.
  The compiler project MUST define exactly `acceptance:compiled-process-plan -> compile-runtime-plan.test.ts`, `acceptance:derivation-handoff -> derivation-handoff.test.ts`, and `acceptance:nx-cache -> nx-cache.test.ts`. Each focused target MUST use `nx:run-commands`, `cache: false`, `parallelism: false`, and `outputs: []`.
  The exact 18-file publication/assembly corpus is `.gitattributes`; `.habitat/AUTHORITY.md`; `.habitat/AUTHORITY-ONTOLOGY.md`; `.habitat/README.md`; `.habitat/blueprints/runtime-compiler/blueprint.toml`; `.habitat/blueprints/runtime-compiler/skill.md`; `.habitat/blueprints/runtime-compiler/structure.toml`; `packages/core/AGENTS.md`; `packages/core/runtime/compiler/AGENTS.md`; `packages/core/runtime/compiler/habitat.toml`; `packages/core/runtime/compiler/project.json`; `packages/core/runtime/compiler/tsdown.config.ts`; `packages/core/sdk/AGENTS.md`; `packages/core/sdk/README.md`; `packages/core/sdk/habitat-pack.json`; `packages/core/sdk/project.json`; `packages/core/sdk/tsdown.config.ts`; and `apps/habitat/test/installed-package.test.ts`. Explicitly exclude `packages/core/sdk/package.json`, any SDK public-face test, product-separation test, root manifest, lockfile, root Nx configuration, and `.habitat/index.json`. The remaining compiler source, tests, and tsconfigs belong only to the distinct implementation closure.
  Stop task 5.1 rather than widening it if implementation would add a compiler export or package identity, an SDK-to-compiler edge, config resolution/decode, `ProviderEffectPlan`, provisioning/acquisition, execution, mounting, observation-port use/publication, a derivation-result/table/portable-artifact input, or any live value.
  Sealed from exact Habitat `main@c8ce735691aa14360c5ea766d05e3fb219250bb0` / tree `96e83ca7881acefd101b394f24e21d2f9abad0c3`: PR #971's exact final head `5edbaa91955be0841172ec604be05ef73ba41a72` changed only the authorized 27-file implementation and publication union; Repository Ratchet run `31453289313` / job `93661823140`, Publish Habitat Ubuntu job `93661822913`, Windows job `93661822832`, and push-to-main Repository Ratchet run `31454155655` / job `93664420237` all passed. Task 5.2 is the sole next proof-only node.
- [x] 5.2 Proof-only: expand only `packages/core/runtime/compiler/test/compile-runtime-plan.test.ts` to prove normalized provider-handoff agreement; required and selected-optional requirement matching; unselected-optional requirement/dependency id retention with its exact derivation finding and no binding/node/edge/resource/reference; missing-finding refusal; dependency closure; dangling-required-dependency refusal; and direct/transitive cycle refusal before result or acquisition. Each invalid compiler input exercised here MUST throw built-in `TypeError` before any `RuntimeCompilationResult`; authored missing/ambiguous provider selection and the sole optional-provider finding remain derivation-owned and are never copied into compiler output, and no `CompilationFinding`, diagnostic result, error API, adapter-target resolution, harness-descriptor compatibility check, provider acquisition, or external mutation is admitted. Change no compiler source, project, blueprint, pack, or public surface. Sealed from exact Habitat `main@659e51d618e1739c9d3b992407ee4a546b89e5d5` / tree `397eb5b0d44eccee146bb1d8cca4c27c1bdc0d02`: PR #973's exact final head `d61a95fb3cefca2a2fcc55c1016fdbedcf153914` changed only the allocated compiler behavior-proof file; its focused test passed 12 tests and 102 assertions, Repository Ratchet run `31456750142` / job `93672013225` passed the affected graph, and push-to-main Repository Ratchet run `31456907948` / job `93672536288` passed the full repository graph. Exact same-resource/different-instance matching, required and selected-optional closure, unselected-optional finding/no-artifact truth, cold handoff disagreement, dangling dependency, and self/transitive cycle refusal are proven without source inspection or downstream work. Task 5.3 is the sole next proof-only node.
- [x] 5.3 Proof-only: expand only `packages/core/runtime/compiler/test/compile-runtime-plan.test.ts` to prove duplicate-check and canonical role agreement; the exact process-role roots and transitive process closure across service bindings, semantic dependencies, resources/providers, workflows, execution refs, and web refs; exclusion of unrelated app-role and semantic facts; and ordinary identity/reference agreement, explicit empty collections, selected harness ids, and no invented adapter target or harness compatibility. Prove the cold reference table preserves exact provider/service definition identity; repeated `providerEntries()` calls return the same one-time, referentially stable, canonically sorted frozen provider snapshot and repeated `serviceEntries()` calls do the same for services, without copying or invoking a definition, descriptor, callback, loader, Effect, provider, or service implementation. Prove `observationSeed` is returned data beside the plan and no observation port enters the input, is consumed or called, or receives a publication. Import/implementation absence remains a reviewed boundary invariant rather than a source-inspection behavior assertion. Change no compiler source, project, blueprint, pack, or public surface. Sealed from exact Habitat `main@72233d03fb6e62643bcbb95e588fff77785e1c93` / tree `3fd22da575888000268cd425e33c87365279b3bd`: PR #975's exact final head `5c50629194a0b728ab632e7cfd3086bfd7d57a6e` changed only the allocated compiler behavior-proof file; its focused suite passed 16 tests and 190 assertions, compiler typecheck passed, and the Nx graph retained exactly the two admitted definition and derivation edges. Repository Ratchet run `31459229200` / job `93679238287` passed the affected graph and push-to-main run `31459364290` / job `93679635534` passed the full repository graph. The receipt proves selected-process closure and unrelated-role exclusion across every admitted channel, canonical/duplicate roles, distinct and overlapping harness sources, exact cold references and stable frozen snapshots, explicit empties, and the observation-seed/no-port boundary with zero authored executable work. Task 5.4 is the sole next proof-only node.
- [x] 5.4 Proof-only: expand only `packages/core/runtime/compiler/test/compile-runtime-plan.test.ts` with TypeScript/TypeBox exact-type and closed-admission fixtures plus equivalent-input determinism. Require surplus/malformed DTOs to fail admission, require the plan/seed DTOs and each DTO collection to preserve §16 freeze/order law, and prove the plan has neither `observationSeed` nor `findings`. Port only behavior assertions from the lab; do not port its public types, Oracle, identity scheme, mutable/truncated plan, diagnostics, or alternate compiler. Runtime source/AST string inspection and a fabricated SDK export are forbidden. Change no compiler source, project, blueprint, pack, or public surface. Sealed from exact Habitat `main@8cf5bb4b72421341963cf6a1f5196a60914b9b6d` / tree `3d175be09bfc070a6fe8651d32e8a9483b05e16c`: PR #977's exact final head `d0e10c435dfafcb796384f1bf73e8a31363c6f7b` changed only the allocated compiler proof file; its focused suite passed 21 tests and 8,197 assertions, compiler typecheck passed, and independent authority plus TypeBox/testing reviews resolved every P0/P1. Repository Ratchet run `31462066861` / job `93687421619` passed the affected graph and push-to-main run `31462195861` / job `93687798949` passed the full repository graph. The receipt proves all sixteen DTO schema/type pairs, closed admission, deep readonly/fresh recursive freeze, exact canonical execution tuples and collection order, authored-order equivalence, absent finding/diagnostic surfaces, input identity/alias nonmutation, built-in-`TypeError` refusal, and zero downstream work without source inspection. Task 5.5 is the sole next proof-only node.
- [x] 5.5 Proof-only: expand only `packages/core/runtime/compiler/test/derivation-handoff.test.ts` to call the real derivation producer, then pass the exact `Entrypoint` used for derivation plus the returned complete `NormalizedAuthoringGraph` to the real compiler. Make producer-local authoring bindings unavailable after handoff, not derivation source; prove success from the artifacts alone and independently refuse every identity/reference disagreement with built-in `TypeError` before compiler result or authored executable invocation. Pass no derivation result, `ExecutionDescriptorTable`, `WebRouteModuleTable`, or `PortableRuntimePlanArtifact`. Change no compiler or derivation source, project, blueprint, pack, SDK/public surface, or test file outside the exact runtime-compiler closure. Sealed from exact Habitat `main@7592bd3edd7cb75cf297211fe620f584c65b8b65` / tree `8371a139957c583fbb160407d783619c1ee514fb`: PR #979's exact final head `9744478c7dbbb3ce50d9429a9ee822e902310a20` changed only the allocated handoff proof file; its focused suite passed 9 tests and 28 assertions, compiler typecheck passed, and independent authority plus testing reviews were clean. Repository Ratchet run `31463985571` / job `93692958332` passed the affected graph and push-to-main run `31464247145` / job `93693718542` passed the full repository graph. The receipt proves a real async/web derivation producer hands only its exact `Entrypoint` and complete returned graph to the real compiler after producer-local authoring bindings leave scope, while all eight app/profile/topology identity disagreements throw built-in `TypeError` before result or Effect/loader work. The compiler sequence is sealed; task 6.0 is now sealed below as the provider-effect-plan authority correction, making task 6.1 the sole next source node.

## 6. Seal Provider Plans And Boot Ordering

- [x] 6.0 Correct provider-effect-plan authority without implementation across exactly nine documents: `HABITAT_ARCHITECTURE.md` as router; `HABITAT_RUNTIME_REALIZATION.md` §13.4 and its directly affected §§17, 25, and 27 as the sole exact mechanics owner; `packages/core/runtime/definition/AGENTS.md` as owner router; and these six active OpenSpec artifacts with `app-runtime-realization` as the sole archive-safe requirement/scenario owner. Classify `ProviderEffectPlan` as `runtime-definition` operational interior rather than a kind, project, or package; freeze the cold curated-Effect, typed acquire, required infallible release, exact build-context/resource-map, private-witness, public-face, successor-blueprint, corpus, failure-boundary, and proof allocation across task 6.1 and substrate tasks 7.2-7.4. Change no `.habitat` file, SDK documentation, implementation, source, test, project, blueprint, package/public output, runtime behavior, or other OpenSpec file. Copy no exact canonical TypeScript block into OpenSpec. Sealed from exact Habitat `main@a142b347144902c657c7203b90d9d53b548b95e5` / tree `f45fba6599dc9161b821d69b79a5b2256381b703`: PR #981's exact head `0b3fabdfef79ccc09b3bc520c14c7f2d11a03287` changed only the authorized nine-document authority surface; strict OpenSpec validation and independent Habitat-authority plus testing/TypeScript/Effect reviews were clean. Repository Ratchet run `31471219815` / job `93714750245` passed the exact PR head, and push-to-main run `31471548022` / job `93715775635` passed the exact merged main SHA. Task 6.1 is the sole next source node.
- [x] 6.1 As the sole next source node, implement and publish the exact task-6.0 provider-plan contract under an independently resolvable `runtime-definition@2`. Preserve `runtime-definition@1` byte-for-byte; create only `.habitat/blueprints/runtime-definition/versions/2/{blueprint.toml,structure.toml}` and select version 2 in the existing definition `habitat.toml`, with no skill, Grit rule, inheritance, fallback, optional interior, version 3, second kind/project/package, or nested `src/providers` directory. Version 2 MUST admit the exact flat successor closure: the existing eight project-shell entries; exactly eleven source files, the existing ten plus flat `src/provider-effect-plan.ts`; and exactly three proof files, `definition.test.ts`, `provider-effect-plan.test.ts`, and `nx-cache.test.ts`.
  The exact implementation/behavior corpus is eight files: definition `src/provider.ts`, `src/provider-effect-plan.ts`, `src/resource.ts`, `src/index.ts`, `test/definition.test.ts`, and `test/provider-effect-plan.test.ts`; derivation `test/complete-derivation.test.ts`; and compiler `test/compile-runtime-plan.test.ts`. Make cold `ProviderFx<TValue, TError>` an exact `HabitatEffect<TValue, TError, never>` value, never a thunk, Promise, or acquired value. Require typed acquire error `TError`; require release, pass it only the acquired value, and require it to return `ProviderFx<void, never>`. `ProviderBuildContext` MUST contain exactly already-decoded `config`, declared-dependency `resources`, and the definition-owned `observation` port, with no scope or telemetry client. Task 6.1 MUST define only the `RuntimeResourceMap` TypeScript contract with `has(requirement)` and the three exact `get(requirement)` overload outcomes; it MUST create no concrete map, instance, factory, iterator, or snapshot. Change `requireResource(...)` to preserve the entire const input type and readonly shape so exact `optional: true`, exact false, and absence remain reachable by those overloads, while widened `boolean | undefined` remains safely `TValue | undefined`.
  `ProviderEffectPlan` MUST expose exactly enumerable `kind`, `acquire`, and `release` keys. Its acquire and release records MUST each expose exactly enumerable `boundary`, `policy`, and `telemetry` metadata, retaining those keys with `undefined` values when optional metadata is absent. Fresh plan, boundary-record, policy, telemetry, and private-witness containers MUST be recursively frozen, while the opaque acquire Effect and release callback MUST retain exact identity and MUST NOT be traversed, copied, frozen, or invoked during construction. The private symbol witness MUST retain those exact bodies through a non-enumerable, non-writable, non-configurable property; its symbol/accessor MUST remain limited to definition assembly and the future Effect substrate and MUST never be projected by the SDK. Frozen `providerFx` MUST expose exactly enumerable `succeed`, `tryPromise`, and `acquireRelease`. The erasure-friendly `RuntimeProvider` interface defaults MUST remain `unknown` config and `unknown` acquire error; only `defineRuntimeProvider(...)` helper inference MUST default schema-free config to `undefined` and acquire error to `never` while adding the required synchronous `build(...)`. Keep plan/build/resource-map contracts TypeScript-only with no fake schema, decoder, or serialized form. Expose no raw Effect, Exit, Scope, Layer, ManagedRuntime, ProviderScope, terminal runner, plan accessor, witness, or alternate constructor.
  Task 6.1 MUST prove only cold construction and type/descriptor law: no `tryPromise` callback, release callback, provider build, or other authored body runs while constructing `ProviderFx`, `ProviderEffectPlan`, provider declarations, derivation artifacts, or compiler output; acquired value/error inference and required `ProviderFx<void, never>` release remain exact; plan metadata and descriptor flags are exact and frozen; opaque bodies retain identity; and the private accessor accepts a real nominal plan and rejects a forged structural lookalike. ManagedRuntime construction remains task 7.1; executing `tryPromise`, live failure classification, real beta.101 `Effect.acquireRelease(acquire, release)` construction/use, and registration immediately after successful acquire remain task 7.2; cleanup observation, rollback, reverse continuation, inert repeated disposal/release, and close remain task 7.3; and qualified-provider live lifecycle conformance remains task 7.4. Compiler and bootgraph carry no provider-plan body or plan.
  The exact 17-file publication/assembly corpus is `.gitattributes`; `.habitat/AUTHORITY.md`; `.habitat/AUTHORITY-ONTOLOGY.md`; `.habitat/README.md`; the two exact version-2 blueprint files; `packages/core/runtime/definition/AGENTS.md`; `packages/core/runtime/definition/habitat.toml`; SDK `src/runtime/providers/index.ts`; SDK `src/runtime/providers/effect/index.ts`; SDK `AGENTS.md`; SDK `README.md`; SDK `habitat-pack.json`; SDK `package.json`; SDK `tsdown.config.ts`; SDK `test/runtime-authoring-public-faces.test.ts`; and `apps/habitat/test/installed-package.test.ts`. The SDK policy pack MUST grow from 14 to 15 sorted members by adding `runtime-definition@2`; copied/input blueprint directories MUST remain exactly 10; JavaScript build specifiers MUST grow from 17 to 18; runtime authoring subpaths MUST grow from 8 to 9. `@habitat-ai/sdk/runtime/providers` MUST expose only value `defineRuntimeProvider` and types `ProviderBuildContext`, `RuntimeProvider`, `RuntimeProviderHealthDescriptor`, and `RuntimeResourceMap`. `@habitat-ai/sdk/runtime/providers/effect` MUST expose only value `providerFx` and types `ProviderAcquire`, `ProviderEffectPlan`, `ProviderFx`, `ProviderFxFacade`, and `ProviderRelease`.
  The complete task-6.1 diff MUST stay within the exact eight-file behavior corpus plus exact 17-file publication corpus, for a ceiling of 25 files. Explicitly exclude `packages/core/runtime/definition/project.json`, definition `tsdown.config.ts`, `.habitat/index.json`, root manifests, the lockfile, root Nx configuration, product-separation acceptance, and every other SDK face. Allocate const-input/readonly requirement preservation, map overload optionality, ProviderFx/error inference, required never-release, no-Promise contracts, and nominal anti-forgery with a positive real-plan assignment plus negative structural-lookalike assignment to TypeScript. Allocate cold no-callback construction, exact plan/facade enumerability and descriptor flags, metadata/witness freezing, opaque body identity, private-accessor forged-witness rejection, and zero-build behavior to owner tests. Habitat and Nx own successor structure/application and unchanged edge/cache behavior; SDK and installed-package proof own exact value/type faces, pack counts/parity/provenance, and installed imports. Use no runtime source-string or AST assertion. Stop rather than widen the task if any requirement needs another file or owner. Sealed from exact Habitat `main@58a05767692e8637e216cde8258184f66cbc10f2` / tree `c9b7044477f397e3c60c014c7584014727f96d56`: PR #983's exact final head `277fcce939b4b9fc4be5af40cecee08e0d30547b` changed exactly the authorized 25-file corpus. Runtime-definition tests passed 20 tests / 434 assertions, runtime-derivation tests passed 22 / 933, runtime-compiler tests passed 31 / 8,291, the SDK public-face suite passed 8 tests, installed-package acceptance passed 5 tests, all focused typechecks/builds and the selected v2 Habitat policy passed, and `bun run check` passed the full repository graph. Independent core-boundary and publication-authority reviews were clean. Repository Ratchet run `31475855872` / job `93729290160` passed the exact PR head; Publish Habitat run `31475855945` passed Ubuntu job `93729290086` and Windows job `93729290010`; and push-to-main Repository Ratchet run `31477239888` / job `93733666035` passed the exact merged SHA. Task 6.2 subsequently sealed the complete bootgraph source node, and task 6.3 is the sole next proof-only node.
- [x] 6.1a Correct runtime-bootgraph authority without implementation across exactly eight documents: `HABITAT_ARCHITECTURE.md` as the ownership router; `HABITAT_RUNTIME_REALIZATION.md` §17 as the sole exact TypeScript, DTO, ordering, refusal, freezing, owner/blueprint closure, corpus, topology, and task-mechanics authority; and these six active OpenSpec artifacts, with `app-runtime-realization` as the sole archive-safe requirement/scenario owner. Freeze task 6.2 as the complete private package-less `runtime-bootgraph@1` owner plus full `orderBootgraph(...)` source, task 6.3 as proof-only expansion of its existing `bootgraph.test.ts`, and task 10.6 as the first real SDK-to-bootgraph source edge. Change no implementation, source, test, project, blueprint, `.habitat` record, package/public output, SDK edge, runtime behavior, or other OpenSpec file, and copy no canonical TypeScript block into OpenSpec. Sealed through authority PR #985 at exact final head `97ea53487d4fdbb5c5b0add744c4ff9f599dd360` and exact merged Habitat `main@038710dcfc0c249d6e376e2f5ad14b7a830e050b`. The exact eight-document corpus was `docs/system/HABITAT_ARCHITECTURE.md`; `docs/system/HABITAT_RUNTIME_REALIZATION.md`; `openspec/changes/realize-app-runtime-spine/proposal.md`; `openspec/changes/realize-app-runtime-spine/design.md`; `openspec/changes/realize-app-runtime-spine/authority-amendment.md`; `openspec/changes/realize-app-runtime-spine/tasks.md`; `openspec/changes/realize-app-runtime-spine/execution-queue.md`; and `openspec/changes/realize-app-runtime-spine/specs/app-runtime-realization/spec.md`. Strict OpenSpec validation and scoped diff checks passed, and independent constitutional plus TypeBox/testing reviews were CLEAN. Repository Ratchet run `31482158724` / job `93749365181` passed the exact PR head; Graphite was clean; no Publish Habitat workflow was scheduled; and push-to-main Repository Ratchet run `31482357946` / job `93749987961` passed the exact merged SHA. Task 6.2 subsequently sealed the complete bootgraph source node, and task 6.3 is the sole next proof-only node.
- [x] 6.2 Create and completely implement the private package-less Nx project `runtime-bootgraph` fresh at `packages/core/runtime/bootgraph` as one complete positive closed `runtime-bootgraph@1`. Its exact project closure MUST be eight root entries (`AGENTS.md`, `habitat.toml`, `project.json`, `src`, `test`, `tsconfig.json`, `tsconfig.test.json`, and `tsdown.config.ts`), exactly four source files (`bootgraph.ts`, `boot-resource-key.ts`, `boot-resource-module.ts`, and `index.ts`), exactly two tests (`bootgraph.test.ts` and `nx-cache.test.ts`), and exactly three blueprint files (`blueprint.toml`, `structure.toml`, and `skill.md`); `src/index.ts` MUST be the sole private assembly interface. Add no `package.json`, optional interior, `versions/`, second version, inheritance, Grit rule, extra source/test, or alternate assembly interface. Define explicitly only `typecheck`, `test`, `build`, and `check`; leave the selected policy and application targets inferred, and add no explicit acceptance, verify, aggregate, or nested scheduler target.
  The exact eight-file implementation/proof corpus is `packages/core/runtime/bootgraph/src/bootgraph.ts`; `packages/core/runtime/bootgraph/src/boot-resource-key.ts`; `packages/core/runtime/bootgraph/src/boot-resource-module.ts`; `packages/core/runtime/bootgraph/src/index.ts`; `packages/core/runtime/bootgraph/test/bootgraph.test.ts`; `packages/core/runtime/bootgraph/test/nx-cache.test.ts`; `packages/core/runtime/bootgraph/tsconfig.json`; and `packages/core/runtime/bootgraph/tsconfig.test.json`. The exact separate 18-file publication/assembly corpus is `.gitattributes`; `.habitat/AUTHORITY.md`; `.habitat/AUTHORITY-ONTOLOGY.md`; `.habitat/README.md`; `.habitat/blueprints/runtime-bootgraph/blueprint.toml`; `.habitat/blueprints/runtime-bootgraph/skill.md`; `.habitat/blueprints/runtime-bootgraph/structure.toml`; `apps/habitat/test/installed-package.test.ts`; `packages/core/AGENTS.md`; `packages/core/runtime/bootgraph/AGENTS.md`; `packages/core/runtime/bootgraph/habitat.toml`; `packages/core/runtime/bootgraph/project.json`; `packages/core/runtime/bootgraph/tsdown.config.ts`; `packages/core/sdk/AGENTS.md`; `packages/core/sdk/README.md`; `packages/core/sdk/habitat-pack.json`; `packages/core/sdk/project.json`; and `packages/core/sdk/tsdown.config.ts`. Their union is the exact 26-file task ceiling.
  Set the sole direct edge to `runtime-bootgraph -> runtime-compiler` through the real relative compiler import and tsconfig reference, never `implicitDependencies`. Implement the full synchronous `orderBootgraph(...)` operation against only compiler-owned `BootgraphInput`. Produce one selection-id-backed resource key per accepted compiler node with exactly the canonical `kind`, `selectionId`, `resourceId`, `lifetime`, optional `role`, and optional `instance` identity, never a requirement owner or `instanceKey`, and one corresponding provider module. Interpret `from -> to` as dependent to dependency. Use Kahn ordering with ascending `selectionId` as the sole ready-node tie-break; emit `modules` and `order` in acquisition order and `rollbackOrder` and `releaseOrder` in exact reverse. Deduplicate repeated dependency targets for a module and sort its dependency keys by `selectionId`, but refuse a duplicate exact `(fromSelectionId, requirementId, toSelectionId)` edge. Create one key object per node and reuse that exact frozen object through its module, every dependency occurrence, `order`, `rollbackOrder`, and `releaseOrder`.
  Treat malformed or surplus input, duplicate `selectionId`, duplicate exact lifecycle resource identity tuple `(resourceId, lifetime, role-or-empty, instance-or-empty)`, duplicate exact edge, dangling source or target, self-cycle, and multi-node cycle as separate caller-reachable refusals that throw built-in `TypeError` before any result. Independently validate every produced artifact against the closed output schema and exact module/order/reverse/reference relations before return; an internal disagreement MUST throw the same generic `TypeError` as a defensive invariant but MUST NOT be allocated a caller-reachable proof fixture. Return a fresh recursively frozen artifact. Neither mutate nor newly freeze input; preserve its preexisting frozen state, property descriptors, and reference identities. Return no findings, diagnostics, partial results, provider references, decoder/redaction metadata, provider-plan/acquire/release bodies, or live values. Do not build or decode a provider, invoke Effect, acquire/release, register a finalizer, publish observation, perform other external work, or admit task-7 behavior.
  Add the exact LF rule for `.habitat/blueprints/runtime-bootgraph/**`. Grow Nx projects 26 to 27; insert `runtime-bootgraph@1` before `runtime-compiler@1` so the sorted protocol-1 SDK pack grows 15 to 16; grow copied/input blueprint directories 10 to 11 and SDK build inputs 13 to 14; keep SDK JavaScript entries exactly 18 and package exports exactly 21. SDK pack/build edits are blueprint asset carriage only: add no SDK source/build edge, source entry, bootgraph public face, package export, or release member. Installed-package acceptance MUST prove pack parity, the exact fixture instance, resolution and application provenance, structure, and restoration. `nx-cache.test.ts` MUST prove unchanged-cache restoration plus relevant-input invalidation. Task 6.2's baseline `bootgraph.test.ts` MUST prove one nontrivial dependency graph's exact ordering, output shape, schema admission, and key-reference reuse plus one representative malformed-input generic `TypeError`, preventing an empty or constant stub from satisfying the complete source node. In that same test, cumulatively prove the old `packages/bootgraph` root and `BOOTGRAPH_RESERVATION` are absent, Nx has no `@rawr/bootgraph`, root workspaces and parsed `bun.lock` contain no old root/package identity, the fresh owner has no `package.json`, and the new graph identity/root and sole edge are exact. Do not edit the frozen product-separation test.
  Explicitly exclude `packages/core/sdk/package.json`, every SDK source/public face and public-face test, `.habitat/index.json`, root manifests, the lockfile, root Nx configuration, `scripts/habitat/product-separation-absence.test.ts`, every file outside the exact 26-file corpus, any package/public identity or SDK edge/export, extra project/blueprint/version/target, source-string or AST proof, config/decode, provider body/build, acquisition/release/finalizer, Effect execution, observation, and task-7 behavior. Task 10.6 alone adds `@habitat-ai/sdk -> runtime-bootgraph` when terminal composition really imports and calls `orderBootgraph(...)`. Sealed at exact Habitat `main@629d08d8b73dc04d2dc999cc020175c671ce14c6` / tree `08e164a31a51b575657b9e39aca1cefb29c181f2`: PR #987's exact final head `fad12967df3b86cd8c99a96daf5fbf9b10495f4d` changed exactly the authorized 26-file corpus. The focused owner test passed 6 tests / 68 assertions, including the isolated cache proof; owner typecheck, build, selected policy/application, SDK typecheck/build, installed-package acceptance, strict OpenSpec validation, and the full 92-task repository check passed. Independent authority and TypeBox/testing reviews were CLEAN. Repository Ratchet run `31485572529` / job `93760059062`, Publish Habitat run `31485572405` with Ubuntu job `93760057750` and Windows job `93760057734`, Graphite mergeability, and push-to-main Repository Ratchet run `31486649785` / job `93763479027` all passed on their exact SHAs. Task 6.3 is the sole next proof-only node.
- [x] 6.2a Correct runtime-bootgraph Proxy-admission authority without implementation across exactly eight documents: `HABITAT_ARCHITECTURE.md` as the ownership router; `HABITAT_RUNTIME_REALIZATION.md` §17 as the sole exact mechanics owner; and these same six active OpenSpec artifacts, with `app-runtime-realization` as the sole archive-safe requirement/scenario owner. Preserve the sealed task-6.2 receipt without reopening or rewriting it. Freeze that neither the input shell nor any nested input record or array may be a Proxy; exact `isProxy` from `node:util/types` MUST detect and refuse active and revoked Proxies before `Array.isArray`, property access or lookup, reflection, schema validation, or any other caller-trapping operation on that candidate. An otherwise ordinary shell, record, or array with a proxied prototype MUST also refuse before any prototype trap, getter, callback, or external work. No path may unwrap a Proxy. Every such refusal MUST be a built-in `TypeError` before result with zero trap, getter, callback, or external-work invocation. Route proof allocation and the exact three-file task-6.2b repair without copying the canonical TypeScript block into OpenSpec. Change no implementation, source, test, project, blueprint, `.habitat` record, config, package/public output, SDK edge, runtime behavior, or other OpenSpec file. Stop rather than widen this exact documentation corpus. Sealed through PR #989 at exact final head `5defc518dca65dc495e0c82d3aa76447ccc84ad6` and exact Habitat `main@2d0dd4b23b87bc06a59bd3146b1a4bf6232a4ff1` / tree `9ca96bb550ec9bc931b7374b232089780c69e2ce`: the exact eight-document corpus changed no implementation, strict OpenSpec validation and diff checks passed, and independent constitutional plus implementability/testing reviews were CLEAN. Repository Ratchet run `31491548480` / job `93778997287`, Graphite mergeability, and push-to-main Repository Ratchet run `31491882562` / job `93780078213` all passed on their exact SHAs; no Publish Habitat run was scheduled. Task 6.2b is the sole next source-bearing repair node; only after it lands does unchanged one-file task 6.3 resume.
- [ ] 6.2b Repair Proxy admission in exactly `packages/core/runtime/bootgraph/src/bootgraph.ts`, `packages/core/runtime/bootgraph/test/bootgraph.test.ts`, and `packages/core/runtime/bootgraph/tsdown.config.ts`. Import exact `isProxy` from `node:util/types` and add `node:util/types` exactly once to the existing `deps.onlyImport` list while retaining `platform: "neutral"`, every existing entry, and every other tsdown option. Refuse every active or revoked Proxy at the input shell or any nested record/array before `Array.isArray`, property access or lookup, reflection, schema validation, or any other caller-trapping operation on that candidate; obtain nested candidates only from already admitted own data-descriptor values, never property access; refuse an otherwise ordinary admitted container whose prototype is a Proxy before prototype traps or inherited lookup; never unwrap. Each refusal MUST synchronously throw built-in `TypeError` before result with zero proxy traps, getters, callbacks, or external work. The behavior test owns real active-Proxy, revoked-Proxy, accessor, and proxied-prototype canaries plus a synchronous non-Promise exact closed-schema-valid successful output unchanged in shape; topology and TypeScript own the non-injectable provider/config/Effect/observation boundary, so add no tautological default-zero counter for work the operation cannot receive. Preserve `platform: "neutral"`, all existing tsdown options, the 8/4/2/3 owner closure, the sole real `runtime-bootgraph -> runtime-compiler` edge, task-6.2's sealed implementation and publication counts, the absent public/SDK face, and every task-7 exclusion. Add no file, dependency edge, package identity, project/blueprint/version/target, SDK/public surface, unwrapping path, finding/diagnostic, config/provider/Effect/observation behavior, or task-7 work. Stop if the repair requires a fourth file, a public or SDK change, another dependency, a platform change, removal or reordering of unrelated `deps.onlyImport` entries, or weakening any task-6.2 invariant. After the focused behavior, TypeScript, topology/build-config, and strict OpenSpec checks pass, task 6.3 resumes its original unchanged one-file proof scope.
- [ ] 6.3 Proof-only: expand only the existing `packages/core/runtime/bootgraph/test/bootgraph.test.ts`. Validate every successful output against the closed schema and exact module/order/reverse/reference relations. Prove permutation-independent Kahn ordering and `selectionId` tie-breaking; target dependency dedupe with duplicate exact edge-triple refusal; exact key-object identity reuse through modules, dependencies, acquisition order, rollback order, and release order; empty and disconnected graphs; fresh recursive output freeze; input neither mutated nor newly frozen with its preexisting frozen state, property descriptors, and reference identities preserved; and every caller-reachable malformed/surplus, duplicate-`selectionId`, duplicate exact lifecycle resource identity tuple `(resourceId, lifetime, role-or-empty, instance-or-empty)`, duplicate exact edge, dangling source/target, self-cycle, and multi-node-cycle built-in-`TypeError` refusal before result. Also prove absent findings/diagnostics, zero provider/config/Effect/observation/external work, and retention of every cumulative deleted `packages/bootgraph` / `@rawr/bootgraph` absence proof. Do not fabricate a caller-reachable internal output-disagreement case: pre-return output/schema agreement remains an implementation invariant. Change no source, project, blueprint, pack, SDK/public surface, `nx-cache.test.ts`, product-separation test, or file outside that one behavior test. Add no task-7 behavior and use no runtime source-string or AST assertion.
- [ ] 6.4 From the held Fluree source quarry, adopt only the path-qualified provider-neutral semantic-ledger contract, Fluree HTTP provider, and conformance behavior recorded in [[stack-cut-sheet#fluree-adoption-ledger]]. Conform the source to the selected resource/provider and `ProviderEffectPlan` law; expose the contract through `@habitat-ai/sdk/resources/semantic-ledger` and the provider through the optional conditional-import subpath `@habitat-ai/sdk/resources/semantic-ledger/fluree`; keep both source projects private and outside release membership. Run `@habitat-ai/resource-semantic-ledger:test`, `provider-semantic-ledger-fluree-http:test`, and installed-package cold-import/optional-peer acceptance before the final runtime release. Prove only the provider-owned config schema/decode contract, cold plan construction, and provider conformance; perform no live acquisition, release, or failure cleanup in this node.
- [ ] 6.5 Re-author only the path-qualified generic temporal-inquiry behavior admitted from the held mixed lineage onto the same released resource/provider law; do not restack or merge that source lineage as a unit. Adopt its provider-neutral contract, concrete `provider-temporal-inquiry-fluree-http` project, and opt-in Nx inference through `@habitat-ai/sdk/resources/temporal-inquiry`, optional `@habitat-ai/sdk/resources/temporal-inquiry/fluree`, and `@habitat-ai/cli`. Run `@habitat-ai/resource-temporal-inquiry:test`, `provider-temporal-inquiry-fluree-http:test`, and `@habitat-ai/cli:acceptance:temporal-inquiry-nx`; prove only the provider-owned config schema/decode contract, cold-plan construction, and provider conformance, with no live acquisition, release, or failure cleanup. In a disposable consumer, run native temporal-inquiry init twice and remove twice: the first invocation of each operation MUST produce the exact declared mutation and the second MUST produce zero mutation. Prove the declared existing project/root receives inferred targets, absence of the manifest is inert, mutable targets are uncached, and no transcript/product policy enters the generic owner. Publish no third package.

## 7. Seal Scoped Acquisition

- [ ] 7.1 Create the private package-less Nx project `runtime-substrate-effect` at `packages/core/runtime/substrate/effect`; inherit the exact task 2.4 scheduler contract, set its exact direct private edges to `runtime-substrate-effect -> runtime-definition`, `runtime-substrate-effect -> runtime-compiler`, and `runtime-substrate-effect -> runtime-bootgraph`, add `@habitat-ai/sdk -> runtime-substrate-effect`, and prove cache hit and invalidation behavior. In that same node, implement exactly one process-owned `effect@4.0.0-beta.101` `ManagedRuntime` from exactly one substrate `Layer.effectContext` lifecycle adapter and expose only `ManagedRuntimeHandle`. Bootgraph order remains ordinary data consumed inside that adapter, never a Layer dependency graph. Force the managed context successfully before any harness mount. `ManagedRuntime` owns its scopes and resource finalization; create no second root `Scope`, second managed runtime, service layer graph, or per-execution runtime. Domain services remain Habitat bound services, not Effect services or Layer nodes. Co-activate the substrate law and prove no other owner can construct a managed runtime.
- [ ] 7.2 Before the first provider acquisition, materialize every declared `env`, `dotenv`, `file`, `memory`, and `test` source and preflight every provider config plus service scope/config ref. Preserve exact authored-order first-hit lookup; skip only absent optional dotenv/file sources, continue past a present source missing the exact key, and refuse an absent required source, absent declared memory/test source, malformed/unreadable source, exhausted key, or winning schema decode failure with zero acquisition and no lower-precedence fallthrough. Then assemble the substrate-private concrete frozen `RuntimeResourceMap` from already-provisioned dependency values with no public factory: `has(...)` and `get(...)` MUST key only by the exact declared `ResourceRequirement` reference, an identity-equivalent copy MUST miss, and required/optional runtime lookup MUST agree with the task-6.1 overload contract. Inside the one `Layer.effectContext` lifecycle adapter and only after a provider's dependencies exist, join the ordered boot artifact to the compiler reference handoff, pass the already decoded full provider-local config through `RuntimeProvider.build(...)`, privately recover the exact plan bodies, and construct and use the real beta.101 `Effect.acquireRelease(acquire, release)` adapter. Prove `tryPromise` maps both synchronous throw and Promise rejection through the authored mapper into typed acquire failure; a failed acquire registers no release; and build throw, forged plan, and Effect defect remain defects rather than typed failures. Apply provider-owned redaction only to diagnostic, telemetry, and catalog projections. Implement `ProvisionedProcess`, register that same plan's release immediately after successful acquisition, await the managed runtime's context before emitting the mount handoff, and prove each selected resource acquires once while preflight, acquisition, or forced-context failure mounts nothing.
- [ ] 7.3 Through the beta.101 `Effect.acquireRelease(acquire, release)` adapter already constructed by task 7.2, execute and prove expected-cleanup recovery and observation inside the provider's infallible release Effect, unexpected-release-defect observation without blocking later releases, startup rollback, reverse release order, release continuation, and inert repeated disposal/release using the `ManagedRuntime`-owned scope/finalizers. Prove `dispose` closes the one runtime after native stop and no separately created root scope exists. Do not construct, lower, or register another release adapter.
- [ ] 7.4 Run the task-6.4 semantic-ledger Fluree HTTP provider and task-6.5 temporal-inquiry Fluree HTTP provider through the real Effect substrate and prove each has single acquisition/release plus failure cleanup. Then port only admitted lab resource-lifecycle fixtures and the generic assertions frozen from Magic consumer oracle `ec7a49c596ca50d5c8ef8ce3f8e3e40cb08c33a7`; prove separate OS processes receive distinct leases and cohosted roles share one process lease. Copy no Magic product wiring, resource catalog, direct provider construction, deployment policy, or dirty worktree evidence.
- [ ] 7.5 Prove the compilation-to-provisioning edge with the real compiled plan and ordered boot artifact, mismatched-artifact refusal before first acquisition, and compiler source unavailable after handoff.

## 8. Seal Process Access And Service Binding

- [ ] 8.1 Create the private package-less Nx project `runtime-process-runtime` at `packages/core/runtime/process-runtime`; inherit the exact task 2.4 scheduler contract, set its exact direct private edges to `runtime-process-runtime -> runtime-derivation`, `runtime-process-runtime -> runtime-compiler`, and `runtime-process-runtime -> runtime-substrate-effect`, add `@habitat-ai/sdk -> runtime-process-runtime`, and prove cache hit and invalidation behavior. In that same node, implement bounded process, role, and surface runtime access from compiled plans and provisioned values; expose no raw Layer, Context, Scope, managed runtime, provider internals, or secrets.
- [ ] 8.2 Author live `BoundService`, `bindService`, `ServiceBindingCache`, cache-key construction, binding assembly, every retained platform-reader cutover, and module-local access narrowing fresh as one `runtime-process-runtime` handoff in one green node. Consume the derived and compiled binding plans and prove actual construction deduplicates exactly on private `{ identity, profileId, bindingId }`, with all five `RuntimeLaunchIdentity` fields. Exclude `NormalizedServiceUse.localName`/the services-map property name as a separate ingredient, plugin/surface/capability, contract/schema object identity, decoded values, and invocation; service-owned dependency keys may affect reuse only through normalized dependency/requirement ids already represented by `bindingId`. Admit no product procedure policy, placeholder adapter, predecessor binding implementation, or alternate live-binding/cache owner.
- [ ] 8.3 Author the execution-facing contracts fresh in their exact `@habitat-ai/sdk/execution` and `runtime-process-runtime` owners, plus the public `@habitat-ai/sdk/effect/context` and `@habitat-ai/sdk/effect/wrap` faces through which the app/process owns the Effect `Context`, resource lifetime, policy, and telemetry decoration without calling an Effect terminal. Prove the deleted workflow seam and predecessor package identity remain absent without a compatibility export.
- [ ] 8.4 Co-activate the process-runtime access law and prove app, entrypoint, service, plugin, adapter, and harness code cannot acquire providers or reach raw handles.
- [ ] 8.5 Implement one process-runtime-owned idempotent process stop handle that releases process-owned resources in reverse dependency order; prove repeated process-stop calls share one operation and never invoke or wait for a harness. Cross-owner native-stop policy and finalization state belong only to runtime mounting in task 10.6.

## 9. Seal Registry And Execution

- [ ] 9.1 Implement `ExecutionRegistry` exact plan/descriptor matching; port the lab's exact-match and post-stop refusal behavior without its runtime constructor.
- [ ] 9.2 Retain one process-owned `ProcessExecutionRuntime` only for invocation classes whose selected native boundary does not already own Effect execution, with cancellation, policy, error, and telemetry bridges. Prove `effect/context` and `effect/wrap` supply the one process-owned environment and decoration without executing the Effect, and prove no oRPC service Effect enters `ProcessExecutionRuntime`; its selected official bridge exclusively owns that request fiber and terminal.

## 10. Seal Adapter, Harness, And Observation Kinds

- [ ] 10.1 Implement `SurfaceAdapter` and its lowering result owner-locally inside `runtime-process-runtime`, with native lowering implementations remaining inside that owner or their selected `runtime-harnesses` owner; create no generic adapter root or Nx project, co-activate the adapter law, and prove lowering performs no execution or mounting.
- [ ] 10.2 Create the private package-less Nx project `runtime-harnesses` at `packages/core/runtime/harnesses`; inherit the exact task 2.4 scheduler contract, set its exact direct private edges to `runtime-harnesses -> runtime-definition`, `runtime-harnesses -> runtime-compiler`, and `runtime-harnesses -> runtime-process-runtime`, add `@habitat-ai/sdk -> runtime-harnesses`, and prove cache hit and invalidation behavior. Consume and re-export the exact definition-owned `RuntimeLaunchIdentity` type without redeclaration. In that same node, implement the generic `HarnessDescriptor`, bounded mount input, read-only required-resource readiness gate, optional distinct process-local liveness/readiness probes, owner-local observation/finding contracts, and `NativeHarnessHandle` with explicit idempotent `stop()`. Re-export only that import-safe companion contract through `@habitat-ai/sdk/runtime/harnesses`; keep `StartedHarness` private to runtime mounting. Co-activate the harness law, define no generic drain protocol or whole-app health/lifecycle controller, and place no Oclif loader or native Oclif harness in the SDK/runtime project. A no-op harness still returns an explicit idempotent no-op native handle and declares health not applicable.
- [ ] 10.3 Prove each generic harness contract in isolation: it mounts only adapter-lowered input, returns one owner-local native handle, shares repeated owner-local stop calls, and never creates `StartedHarness`, releases process resources, or projects observation read models.
- [ ] 10.4 Prove process runtime emits the provisioning-to-mounting handoff as mount-ready records plus its own stop handle, refuses mismatched artifacts before native mutation, and no longer needs provider/compiler source after handoff; it MUST NOT invoke a harness.
- [ ] 10.5 Create the private package-less Nx project `runtime-observation` at `packages/core/runtime/observation`; inherit the exact task 2.4 scheduler contract, set its sole direct private edge to `runtime-observation -> runtime-definition`, add `@habitat-ai/sdk -> runtime-observation` and the sole public `@habitat-ai/sdk/runtime/observation` face, and prove cache hit and invalidation behavior. In that same node, implement the definition-owned observation port plus `RuntimeDiagnostic`, `RuntimeCatalog`, and `RuntimeTelemetry` as non-authorizing read-model projection. Prove observation failure cannot select, acquire, mount, stop, or replace product outcomes.
- [ ] 10.6 Create the private package-less Nx project `runtime-mounting` at `packages/core/runtime/mounting`; inherit the exact task 2.4 scheduler contract, set its exact direct private edges to `runtime-mounting -> runtime-definition`, `runtime-mounting -> runtime-process-runtime`, and `runtime-mounting -> runtime-harnesses`, add `@habitat-ai/sdk -> runtime-mounting`, and prove cache hit and invalidation behavior. The SDK composition root supplies the definition-owned observation port implementation; `runtime-mounting` MUST NOT import `runtime-observation`, `runtime-compiler`, or `runtime-bootgraph`. In the same node, the terminal SDK `startApp(...)` composition source MUST really import and call both `compileRuntimePlan(...)` and `orderBootgraph(...)`, thereby establishing the final direct `@habitat-ai/sdk -> runtime-compiler` and `@habitat-ai/sdk -> runtime-bootgraph` assembly edges; transitive process-runtime reachability and blueprint asset carriage MUST NOT substitute. Implement live `startApp(...)`, harness invocation, creation and ownership of private `StartedHarness`, reverse native stop, process-stop coordination, and the sole process-local cross-owner single-flight finalization state machine with closed `running`, `draining(deadline, pendingNativeStop)`, and `settled` states plus the sole admitted `waitForNativeStop` policy. Consume the exact `Entrypoint` artifact supplied to `startApp(...)`, use its already-frozen `RuntimeLaunchIdentity` without reconstructing selection or accepting a separate identity input, force the one managed context before mount, and keep optional liveness distinct from fail-closed readiness over every required resource and selected required harness contribution. Expose `startApp(...)` only through the terminal SDK facade. Each invocation owns only its selected process lease, managed runtime, resources, native handles, identity, and stop. Prove repeated stop calls for one process share one operation; sibling process stops, restarts, health, and observations remain independent; truthful deadlines never imply native work settled when it did not; adapter failure mounts nothing; later mount failure stops the mounted prefix once; the mount failure stays primary; and providers release only after native stops settle. Add no whole-app handle, readiness aggregate, supervisor, deployment controller, or sibling control.
- [ ] 10.7 Prove the mounting-to-observation edge with real `StartedHarness` records, mismatched-record refusal before observation publication, native mount source unavailable after handoff, and process-runtime source unavailable after its stop handle and mount-ready records cross into runtime mounting.

## 11. Realize Habitat Through The Oclif Vertical

- [ ] 11.1 Bind the already qualified `services/catalog` / `@habitat-ai/catalog-service` owner and every retained reader into the Habitat self-host; prove the predecessor path and package identity remain absent and change no tool identity in that node.
- [ ] 11.2 Normalize `scripts/habitat` Nx metadata as exactly one `type:tool` / `role:architecture-policy` project, remove its application classification, and prove the repository-wide project inventory contains no second project rooted there; change no service or app identity in that node.
- [ ] 11.3 Author complete, independently resolvable `app@2` as the future successor without changing one byte of immutable `app@1`; pack and installed-consumer proof MUST resolve each complete version by its exact locator with no inheritance or fallback. Define the one-project self-hosted `apps/habitat/habitat.app.ts`, its `runtime/profiles/*`, finite `runtime/processes.ts` catalog, `cli.ts` process entrypoint, and selected source-inventory and rule-evaluation providers. Require the entrypoint module to produce exactly one `Entrypoint` through `defineEntrypoint(...)`, thereby selecting one catalog record, and pass that exact artifact to its sole terminal SDK `startApp(...)` call without reconstructing selection or identity; co-activate the positive app/profile/process-catalog/entrypoint law. Command-topic membership waits for the conforming topic owner in task 11.4 rather than creating an ownerless selection. Create no process child project or `process`, MCP, or async-server kind.
- [ ] 11.4 In one atomic Oclif vertical, create `plugins/cli/topics/foundation` as the private `@habitat-ai/plugin-foundation`, select that topic into the Habitat app/profile, implement the Oclif `SurfaceAdapter` lowering inside `runtime-process-runtime`, the foundational Oclif loader/native harness inside `@habitat-ai/cli`, and the import-safe `@habitat-ai/cli/host` consumer entrypoint; define the topic-neutral private CLI source-bundle contract consumed by that loader; realize the topic's one real `habitat resolve` command through all seven phases; and wire the `acceptance:oclif-native-runtime` Nx target to the complete built-child-process matrix. Use low-level `run`, root `finally`, awaited output flush, cancellation, and `handle` last; prove source/installed parity, exact selected inventory, registry/adapter/harness observations, real catalog/resource execution, one process runtime, one provider acquisition, success with zero `handle` calls, declared failure code `2`, command-primary cleanup failure code `2`, SIGINT/SIGTERM numeric code `1`, exact call order, complete output, and no surviving handle. First co-land and activate the final complete positive closed Oclif-app and CLI-topic laws plus the private source-bundle contract only in this passing node; prove the predecessor `apps/cli`, `plugins/cli/commands`, Rawr package families and niches, legacy `forbids`, alternate-launcher, and compatibility assertions are absent. This foundational vertical creates no agent-plugin topic, overlay, or profile. No second Oclif adapter, loader, harness, source-bundle contract, app law, or topic law may exist.
- [ ] 11.5 Add `habitat check` and `habitat hook` to the same private `@habitat-ai/plugin-foundation` topic and realize them through the same seven-phase vertical, move every Habitat client and entrypoint reader, and delete module-global `createHabitatClientForWorkspace` acquisition in the same node.
- [ ] 11.6 Implement the two native `@habitat-ai/cli` Nx generator entrypoints and their shared qualified verified-write mechanics without creating a command topic or Oclif projection. Co-land `@habitat-ai/cli:acceptance:generators-installed-package`: from local tarballs in a disposable consumer, invoke both generators through native `nx generate`, exercise exact-existing convergence and divergent-path refusal, and prove compilable Habitat/public-extension output, exact Nx registration, complete atomic writes, and zero refused-write mutation without importing Rawr templates, destinations, package identities, command bases, or topics.
- [ ] 11.7 Implement child-process fixtures only beside the CLI or semantic owner whose behavior they verify; prove the deleted generic test-utils package and every predecessor reader remain absent.
- [ ] 11.8 Pack local `@habitat-ai/sdk` and `@habitat-ai/cli` tarballs, publish them only to the isolated acceptance registry, and install-smoke the Habitat Oclif vertical through the same one-operation `nx add` acceptance targets; verify byte-identical independently resolvable `app@1`, complete independently resolvable `app@2`, every public subpath including `@habitat-ai/sdk/runtime/harnesses`, packed external-companion attachment without a private runtime import, and no internal runtime package cohort. This is a pre-release artifact check, not public-registry publication.

## 12. Realize Additional Habitat CLI Verticals

Every task in this section extends the already selected Habitat self-host. No
downstream product command, app, manifest, or compatibility path enters this
repository.

- [ ] 12.1 Create the final `plugins/cli/topics/agent-plugins` owner as the private
  `@habitat-ai/plugin-agent-plugins`, re-author the qualified
  `.habitat/overlays/agent-plugin-lifecycle` policy there, and add its command
  membership plus cold content-workspace/package-output/native-provider/
  versioned-content and telemetry provider selections to the Habitat app
  profile. Realize every retained `habitat agent plugins` command through the
  sealed Habitat command contract and task 11.4 private source-bundle contract,
  explicit host/workspace binding, the already-qualified agent-plugin lifecycle
  service/resources/providers, runtime mounting, and non-authorizing
  observation. The qualified telemetry resource/provider from task 3.1 and the
  real Oclif telemetry integration MUST already be accepted or MUST co-land
  atomically here with `@habitat-ai/cli:acceptance:oclif-native-telemetry`; no
  placeholder provider, backend, topic, profile, or command bundle is admitted.
  Through the installed binary, invoke `check`, `package`, `status`, `sync`,
  `test`, and `vendors update` against disposable real Git/provider homes and
  assert each command's exact result, refusal, mutation prefix, output, exit
  classification, and correlated telemetry receipt. Repeat the converged native
  operation with zero mutation, acquire one process lease per invocation, and
  settle one Oclif harness plus all resources in canonical reverse order. This
  is the first final agent-plugin topic, overlay, app-profile selection, and
  installed agent-plugin acceptance; task 2.11 deletion is not its proof.
- [ ] 12.2 Re-author the final `services/dev` / `@habitat-ai/dev-service`
  owner, its explicit filesystem, path, process, and clock ports, and
  `plugins/cli/topics/dev` from accepted contracts plus the task 2.11 frozen
  evidence; then realize `habitat dev repo`, `habitat dev stack`, and `habitat
  dev worktree` through those final owners and the same Oclif/runtime path.
  Through the installed binary and app-selected Effect Platform Node provider,
  prove each family's exact dry, admitted, and refused result, mutation prefix,
  output, exit classification, source/compiled command membership, and absence
  of the deleted `packages/dev-node` reader path. Delete no behavior by alias or
  fallback; the removed root doctor, HQ, reflect, routine, tools-export,
  workflow-harden, config, journal, security, hello, and agent-plugin-create
  commands remain absent.
- [ ] 12.3 Create `plugins/cli/topics/authoring` as the private `@habitat-ai/plugin-authoring`, select it into the Habitat app, and realize exactly `habitat cli command create` and `habitat cli extension create` as thin projections over the task 11.6 native `@habitat-ai/cli` Nx generator entrypoints. Extend and pass `@habitat-ai/cli:acceptance:generators-installed-package` through the installed command surface; prove exact topic/source-bundle membership, generated output targeting only Habitat/public-extension contracts, the one Habitat command contract, and absence of every Rawr template, path, package identity, environment variable, command base, partial write, aggregate factory, or manifest/project drift.

## 13. Realize Qualified Server And Async Harnesses

Tasks 13.1 through 13.5 use indispensable owner-local Habitat conformance
fixtures. A fixture does not become a production app, service, plugin, or
published example project.

- [ ] 13.1 Add the Elysia adapter/harness with an owner-local conditional dynamic import and Elysia optional peer metadata co-landed in this first conforming `runtime-harnesses` host owner; task 3.4 creates none of those surfaces. Add an owner-local live gated-request fixture and the exact `runtime-harnesses:acceptance:server` target: after one request is admitted, begin one `stop(false)` and prove a second connection is rejected while the first remains gated; observe the deadline, shared repeated-stop operation, no release/dispose/completed-drain claim, then unblock the first request and prove one release/dispose. Never escalate to `stop(true)`; WebSocket drain is required only when the fixture selects a WebSocket surface.
- [ ] 13.2 Through the Elysia/oRPC owner-local fixture, exercise one synchronous or Promise-returning non-Effect operation through inline native `.handler` and one Effect-backed operation through the selected official implementation-owned `@orpc/experimental-effect@2.0.0-beta.23` `.effect` extension installed once in `src/service/impl.ts` through `@habitat-ai/sdk/plugins/server/effect`. Prove that `.effect` owns the request-fiber signal, Cause reconciliation, `Effect.runPromiseExit`, and Promise boundary, with `handlerGen` only its internal underlying vendor mechanism; a real aborted request interrupts the Effect and preserves oRPC's native outcome; `effect/context` plus `effect/wrap` supply the app/process-owned Context, resource lifetime, policy, and telemetry; acquired resources release once after native settlement; and oRPC, the bridge, and Effect resolve through one module realm. Fail on a direct Habitat-authored authoring, adapter, or operation import/call of `handlerGen`, Habitat imitation, manual/custom runner, service- or adapter-authored direct `Effect.run*`, or any oRPC service Effect entering `ProcessExecutionRuntime`; the official extension's internal delegation remains admitted. Retain no app composition or product reader.
- [ ] 13.3 Add native `inngest@4.18.0` Serve and Connect harness modes plus `FunctionBundle` with an owner-local async conformance fixture; explicitly reject `effect-inngest`. The task 13.3/13.4 `runtime-harnesses` sequence owns the first conforming Inngest host and MUST co-land its optional peer metadata and owner-local conditional dynamic import; task 3.4 and the task 4.2 host-neutral async face create neither. Keep bundle registration factories private, materialize them with exactly the same native client supplied to the selected Serve or Connect harness, and add no `dispatcherDescriptor` unless a separately named consumer requires it; `WorkflowDispatcher` remains a separate named consumer/materialization. Through `runtime-harnesses:acceptance:inngest-native-telemetry`, execute a real function with the exact boundary `step.run(id, () => ProcessExecutionRuntime...)`; prove native retry, memoization, and history. Prove replay re-enters the native function and `step.run` registration rather than resuming a suspended Effect fiber; a completed memoized step returns native memoized state without invoking the callback or `ProcessExecutionRuntime`, while a failed or otherwise un-memoized attempt invokes the callback anew. Native cancellation MUST retain its real semantics: it does not interrupt an already active step and Habitat injects no synthetic cancellation signal. Serve MUST track every admitted handler Promise and wait for its admitted-handler tracker to reach zero before provider release. A mocked worker, protocol emulator, source inspection, Effect-owned retry, or `effect-inngest` path is not acceptance.
- [ ] 13.4 Through disposable native `connect({ handleShutdownSignals: [] })`, prove zero process-listener delta and keep the owner-local callback tracker required by exact 4.18 source: `RequestProcessor.handleExtendLeaseAck` deletes `requestLeases` when renewal is denied while explicitly allowing the user callback to continue; `ConnectionCore.close` and `reconcileLoop` gate on `requestLeases`; `waitForInProgress` exists, but `SameThreadStrategy.close` does not call it. Prove one outer mounting-owned single-flight stop shared by repeated/concurrent callers; it MUST invoke and await native `close()` once, then wait for owner callback-tracker zero before provider release, without a Connect test double. Native close/flush never proves delivery or callback completion. Emit only evidence-backed `presented`, `confirmed`, `dropped`, or `unknown` observations and retain the native Inngest outcome. Retain no app composition, product reader, synthetic signal, or second shutdown owner.
- [ ] 13.5 Through one indispensable owner-local `app@2` fixture and one app-owned finite `ProcessCatalog`, launch a real built Elysia `server` child and native Inngest Serve `async` child from separate thin entrypoint modules that each produce one `Entrypoint` through `defineEntrypoint(...)` and pass that exact artifact to one `startApp(...)` call without reconstruction. Prove each child accepts one real native boundary operation and owns the distinct immutable `RuntimeLaunchIdentity` carried by its artifact, process lease, beta.101 managed runtime, resources, native handle set, and stop. Stop and restart each child independently; stopping either MUST settle its native stop before resource release and MUST NOT invoke, await, observe, release, or restart the sibling. Remove one required async resource and prove async provisioning fails before mount while the server remains live. With both mounted, prove liveness and readiness remain distinct, process-local, fail-closed, and carry each child's exact deployment/source identity. Create no child Nx project, `process`/MCP/`async-server` kind, supervisor, deployment unit, cross-process lifecycle/health controller, or production example app.
- [ ] 13.6 **Conditional external companion only.** If and only if `mcp-openapi@1.0.0` is available as an independently versioned ordinary package artifact, attach that package through `@habitat-ai/sdk/runtime/harnesses` to one intentional MCP `server` surface/process projection and add owner-local packed-artifact acceptance for its real tool and OpenAPI-resource surface, process-local liveness/readiness, immutable launch identity, and native stop-before-release behavior. Use only the companion's public lifecycle; copy no Magic tarball or source and implement no direct official MCP SDK server in Habitat. Claim no prompts surface. If no independent artifact exists, record the condition as unsatisfied and admit no MCP dependency, MCP authoring face, MCP-specific SDK subpath, MCP adapter, MCP harness, or MCP release claim; preserve the already-required generic `@habitat-ai/sdk/runtime/harnesses` public contract unchanged. MCP remains neither a role, kind, app, service, provider, deployment unit, nor lifecycle owner, and this conditional task does not block the core runtime release.

## 14. Realize The Qualified Web Harness

- [ ] 14.1 Add the generic web adapter/harness build handoff with an indispensable owner-local Habitat conformance fixture; create no browser managed runtime without a selected browser resource.
- [ ] 14.2 Prove the fixture's build output and native mount handoff through the terminal SDK web face and exact `runtime-harnesses:acceptance:web` target, then keep only the minimum owner-local fixture required by that acceptance. The deleted Vite/React product surface, `apps/web`, and `@rawr/web` identity remain absent.

## 15. Audit, Handoff, Land, And Drain

- [ ] 15.1 Audit the telemetry checkpoint, acceptance receipts, and held-source
  retirement only. Verify that task 3.1 already owns the qualified telemetry
  resource/provider and singleton retirement; the exact runtime owner nodes
  already own passing `runtime-process-runtime:test`,
  `runtime-mounting:test`, and `runtime-observation:test`; task 12.1 already
  records the app-selected installed-Oclif
  `@habitat-ai/cli:acceptance:oclif-native-telemetry` receipt; the server owner
  records `runtime-harnesses:acceptance:server-native-telemetry`; and task 13.3
  owns the async counterpart. A later aggregate MAY depend on these targets but
  MUST NOT replace them. This audit adds no provider implementation, backend
  activation, app/profile selection, Oclif wiring, acceptance placeholder, or
  delayed authority transfer. Retire the held mixed source only when every
  destination receipt is exact, accepted, and mapped to its final owner;
  downstream products consume only the later released integration from their
  own repositories.
- [ ] 15.2 Audit and remove only unreachable guidance, metadata, tests, and files left after same-node cutovers; the audit MUST NOT perform a delayed authority transfer. Extend the finite cumulative separation-absence inventory in the same node as any newly admitted deletion.
- [ ] 15.3 Run repository-wide Habitat topology, Nx graph, import reachability, exact final SDK export closure, Oclif manifests, lint, typecheck, test, build, local package, installed-package checks, and `nx run habitat:acceptance:product-separation-absence`. Require byte-identical independently resolvable `app@1`, complete independently resolvable `app@2`, exactly one app/Nx project per app, one finite cold process catalog, thin entrypoint modules that each produce one `Entrypoint` through `defineEntrypoint(...)` and pass that exact artifact to one `startApp(...)` call without reconstruction, one beta.101 ManagedRuntime built from one `Layer.effectContext` lifecycle adapter per started process, forced context before mount, distinct same-app built Elysia/native Inngest process leases and stops, the public packed companion-harness contract, a deployment-safe cold plan handoff, and distinct process-local fail-closed liveness/readiness carrying immutable launch identity. Require `apps/habitat` to be Habitat's only production app identity and require no Rawr product source, package identity, superseded capability spec, product documentation, compatibility path, production example project, new process/MCP/async-server kind, MCP authoring face, direct MCP SDK implementation, or cross-process lifecycle/health controller except the exact canonical removal targets `openspec/specs/{rawr-cli-application,legacy-membership-retirement,mixed-plugin-lifecycle-retirement}/spec.md`. A conditional external `mcp-openapi@1.0.0` receipt is required only if task 13.6's independent-artifact condition was satisfied. Those targets, this active canonical change, and their current removal deltas remain only until task 15.9 applies and archives them.
- [ ] 15.4 Record architecture and structural-quality review for every boundary-changing semantic node. Record TypeScript and behavior-first test review for each source-bearing node. Add Oclif, Effect, oRPC, or Inngest review only when that node touches the corresponding vendor boundary; add MCP companion review only if task 13.6's independent-artifact condition is satisfied. Resolve and record every P0/P1 disposition before landing each owner/artifact handoff and its activated law as a truthful Graphite node; never create a law-only red node, deferred deletion node, or oversized aggregate.
- [ ] 15.5 Land the complete Habitat runtime stack on canonical `main`, pass the exact-main Repository Ratchet and canonical CI, and record the exact source commit. Only this landed exact-main revision may authorize the final runtime release; it does not retroactively replace the bounded pre-separation foundation-release authority at task 2.8 Gate B.
- [ ] 15.6 From that exact Habitat `main`, use the existing Nx Release group for the second and final release of this runtime-spine change: tag, publish, and registry-install-smoke exactly `@habitat-ai/sdk` and `@habitat-ai/cli`. Assemble the qualified telemetry provider only into `@habitat-ai/sdk/telemetry`, the semantic-ledger contract/provider only into `@habitat-ai/sdk/resources/semantic-ledger` and its optional `/fluree` integration, and temporal inquiry only into its equivalent SDK subpaths; exercise every subpath through installed-package acceptance. The receipt MUST name byte-identical `app@1`, complete `app@2`, `@habitat-ai/sdk/runtime/harnesses`, the beta.101 substrate proof, built Elysia/native Inngest same-app isolation, deployment cold-handoff proof, and process-local readiness identity contract. Include an external `mcp-openapi@1.0.0` attachment receipt only if task 13.6's independent-artifact condition was satisfied; its absence MUST NOT be replaced by a Habitat MCP implementation or block the core release. Publish no third telemetry package, internal runtime package, resource/service/plugin cohort, `@habitat-ai/plugin-agent-plugins`, or Rawr package. Record exact versions, source commit, package provenance, and integrity.
- [ ] 15.7 Publish the exact runtime/package checkpoint and lane-specific informational migration handoffs to the already separated Rawr owner, research-service, Civ7, Magic Migration, Inngest, MCP-companion, and oRPC lanes. Shape each handoff for its consumer through the prompt-design review lens, but state released facts and consumer-owned acceptance rather than scripting product implementation. The common handoff/return contract MUST record exact release provenance and integrity, native migration plus idempotence, resolved public surfaces, the byte-identical `app@1` / complete `app@2` distinction, process catalog and launch-identity/readiness/isolation proof, task 13.6's MCP-companion disposition, the consumer receipt identity, and the rule that a prototype is deleted only after its replacement receipt is accepted. Because Rawr installed the initial plugin release through task 2.9, its owner-local OpenSpec MUST generate and apply this later `@habitat-ai/cli@<released-version>` upgrade through `nx migrate`; the migrated CLI dependency supplies the exact `@habitat-ai/sdk` version. A handoff MAY tell a consumer without Habitat that the one native entry operation is `bunx nx add @habitat-ai/cli --no-interactive`. Rawr performs its own corpus, Hyperresearch, and session-intelligence runtime acceptance, and consumers install or migrate rather than copy internal owners, Magic product wiring, or a vendored companion tarball.
- [ ] 15.8 Through separate Rawr owner-local records after the final Habitat release, adopt the workstream domain against `@habitat-ai/sdk/resources/semantic-ledger`, re-author the accepted research-experiment service against the released runtime, and transfer the unique authority-freeze/specification-toolbox references as non-executable guidance. Pass the named Rawr targets in [[classification-ledger#behavioral-acceptance-matrix]] and retire each Habitat-side source only after destination acceptance; retire the mixed Fluree root only after both its Habitat and Rawr sinks have landed.
- [ ] 15.9 Re-run exact-main gates in Habitat and Rawr after consumer migration and later-product acceptance, and verify Marketplace remains an independent content-only repository. While the three canonical removal targets `openspec/specs/{rawr-cli-application,legacy-membership-retirement,mixed-plugin-lifecycle-retirement}/spec.md` still exist, invoke the pinned OpenSpec 1.3.1 archive workflow with its documented `--no-validate` option for these full-capability removals: the native archive cannot validate intentional intermediate empty targets. Require the archive to apply every delta and preserve all three complete removal records, delete only those now-empty retired canonical targets, then normalize exactly the three retained Purpose blocks recorded in [[design#canonical-purpose-normalization]] and immediately run strict canonical validation. Prove no superseded capability spec, Personal/Template product-era authority, or Rawr-positive command/application law remains. Any incomplete archive, nonempty retired target, purpose mismatch, unrelated main-spec mutation, or strict-validation failure restores the pre-archive Git tree and fails the node. Record that every retained or transferred stack has landed at its destination, record the explicit retirement allowlist, and release only clean completed worktrees. Retire remaining unsubmitted superseded sources explicitly, then run `gt sync --force --no-restack --no-interactive` separately from canonical `main` in each Graphite repository to prune only the already-dispositioned residue. Verify Habitat, Rawr, Marketplace, Graphite, worktrees, releases, and runtime state are clean. This task performs no source extraction, delayed ownership transfer, or deletion of an occupied/undispositioned branch.
