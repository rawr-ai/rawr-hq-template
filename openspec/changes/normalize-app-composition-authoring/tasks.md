## 1. Scope Correction And Record Gate

- [x] 1.1 Record the user's categorical correction: C4 retires the legacy mixed web/state lifecycle and does not implement app composition, app-projection generation, mounting, runtime realization, or runtime status.
- [x] 1.2 Bind the corrected record to the accepted packet provenance, the superseding repository-separation amendment, landed C1/C2 authority, and the two canonical architecture-migration specifications.
- [x] 1.3 Replace the unlanded `app-composition-authority` delta with `legacy-membership-retirement`; reduce qualified authoring from four creators to exactly three; rewrite proposal, design, tasks, objective, falsifiers, authority ledger, proof boundary, and closure honestly.
- [x] 1.4 Pass pinned strict OpenSpec validation and `git diff --check` for the corrected record after implementation edits settle.

## 2. Remove Superseded C4 Runtime Work

- [x] 2.1 Remove the C4 composition show/select/unselect/check commands, structured AppDefinition source adapter, and composition application code.
- [x] 2.2 Remove the C4 `@rawr/hq-sdk/app-composition` identity/snapshot domain, app role materializers, web projection loader/readiness path, composition observation endpoint, and runtime status integration.
- [x] 2.3 Remove the C4 app-projection creator and its command, generator, fixtures, tests, metadata, and architecture requirements.
- [x] 2.4 Restore only the minimum existing host/server/web behavior needed to compile and preserve unrelated regressions; do not add a replacement registry, snapshot, mount, realization, or status owner.
- [x] 2.5 Add a structural gate proving the superseded mini-runtime is absent and both canonical architecture specifications remain references rather than C4 implementation inputs.

## 3. Retire Legacy Mixed Membership

- [x] 3.1 Delete `plugins/cli/plugins/src/commands/plugins/web/**` and prove every former command ID, alias, hidden alias, help row, active doc, and tool-registry entry is absent. Preserve the inherited mixed sync aggregate and root undo unchanged as migration evidence for C5 rather than claiming the external-only cutover in C4.
- [x] 3.2 Delete `services/hq-ops/src/service/modules/repo-state/**`, membership storage/lock helpers, contract/router/base exports, fixtures, and positive structural guards.
- [x] 3.3 Delete `plugins/server/api/state/**`, its registration and host wiring, browser client dependency, tests, package/workspace metadata, and positive topology guards.
- [x] 3.4 Remove `.rawr/state/state.json` and `plugins.enabled` producers/consumers, workspace scan/filter mounting, `/rawr/plugins/web/*`, state-driven mounts UI, ID-to-directory conversion, variable-path imports, and their tests without a compatibility reader or replacement mount path.
- [x] 3.5 Prove stale state is ignored rather than read, migrated, repaired, synchronized, or deleted; prove the retired procedure/path is absent from composed RPC/OpenAPI while representative surviving behavior remains callable.

## 4. Three Qualified Authoring Applications

- [x] 4.1 Implement kind-agnostic destination containment, qualified relative paths, deterministic complete plans, exact-byte comparison, no-replace publication, and closed truthful results; shared code accepts no raw product identity and selects no kind or authority.
- [x] 4.2 Implement `rawr cli command create <topic> <name>` through a separate owner that verifies exact Template repository identity, writes only official command source/test/metadata, rejects personal/foreign roots, and leaves controller selection unchanged.
- [x] 4.3 Implement `rawr cli extension create <id> --destination <path>` through a separate portable owner that requires an explicit destination, produces a self-contained external Oclif extension outside Nx/RAWR workspaces, and never mutates the Oclif registry.
- [x] 4.4 Implement `rawr agent plugins create <id> --content-workspace <path>` through a separate owner that verifies personal curated-content identity and writes only canonical source/declarative content, with no Template implementation, lifecycle record, artifact, provider, export, package, controller, or runtime state.
- [x] 4.5 Prove raw argument parsing, identity grammar, templates, and repository/destination policy remain per-kind; no cross-kind import, aggregate `mode|kind|type` dispatch, shared raw-identity parser, or inferred authority exists.
- [x] 4.6 Prove dry-run equality, divergent-plan rejection before writes, exact-existing zero-write/zero-metadata convergence, first-publication empty-prefix failure, later-publication truthful partial result, and retry behavior.

## 5. Delete Legacy Scaffold Aggregate

- [x] 5.1 Delete `plugins/cli/plugins/src/commands/plugins/scaffold/**`, the mixed factory, obsolete factory/scaffold tests, and every factory-specific import/export/help/doc/tool-registry route after the three qualified owners independently retain only their source-authoring capabilities.
- [x] 5.2 Prove command discovery contains exactly the three qualified creators and no scaffold, composition, app-projection, alias, forwarding command, or output-kind selector.
- [x] 5.3 Trap install, composition, build, package, export, sync, provider, promotion, process, controller, and native Oclif mutation ports while all three creators run; every adjacent counter remains zero.

## 6. Architecture And Semantic Rails

- [x] 6.1 Add permanent direction gates for three thin commands, three per-kind applications, and narrow shared qualified write-plan primitives.
- [x] 6.2 Reject cross-kind imports, product dispatch in shared code, C2/C3 stateful service imports, native Oclif mutation, `@rawr/plugin-plugins` implementation imports, personal executable paths, and any replacement composition/runtime owner.
- [x] 6.3 Replace positive guards that require repo state, state API/UI, web scans/mounts, or legacy web/scaffold commands with target-state semantic-absence gates rather than weakening coverage.
- [x] 6.4 Update exact Template command/tool metadata and C5 handoff records without compatibility vocabulary, app-runtime completion claims, or cross-repository integration claims.

## 7. Verification And Standing Review

- [x] 7.1 Run focused behavior suites for legacy retirement and all three creators, including exact Template/personal/foreign Git fixtures and foreign-directory external-extension build/test acceptance.
- [x] 7.2 Run surviving server, web, RPC/OpenAPI, HQ app, and Inngest regression suites needed by the touched deletion boundary without exercising protected candidate bytes.
- [x] 7.3 Run uncached build, typecheck, lint, test, structural, and exact Nx affected targets for every changed project.
- [x] 7.4 Run command ontology, semantic absence, zero adjacent mutation, zero-write convergence, strict OpenSpec, and `git diff --check` proof from the recorded C4 main base.
- [x] 7.5 Cycle standing TypeScript/refactoring, architecture/authority, behavior-first testing, and structural-quality reviewers over the corrected complete slice; repair or evidence-disposition every invariant, supported-behavior, or material-maintainability finding.
- [x] 7.6 Rerun the complete corrected proof after final review repair with no superseded runtime artifact or active legacy route remaining.

## 8. Landing And Closure

- [ ] 8.1 Commit and submit reviewed C4 source through Graphite with `--ai`, merge to canonical Template `main`, and rerun exact post-main OpenSpec, architecture, affected-graph, command, semantic-absence, and foreign-directory proof.
- [ ] 8.2 Synchronize the two truthful capability deltas and archive the completed C4 record only after source landing through the guarded OpenSpec path.
- [ ] 8.3 Land the archive record through Graphite, record exact reviewed/landed commits, trees, PRs, and post-main proof, then drain every C4 Graphite node and worktree before C5 depends on C4.
- [ ] 8.4 Close C4 with an explicit statement that legacy web/state membership and scaffold ownership are retired and three source creators are qualified, while aggregate lifecycle cutover, app composition, and runtime realization remain deferred and unclaimed.
