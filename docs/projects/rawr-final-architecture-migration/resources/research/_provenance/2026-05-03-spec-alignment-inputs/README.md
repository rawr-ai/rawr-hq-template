# 2026-05-03 Specification Alignment Inputs

Status: preserved inputs / provenance / not active guidance.

This directory preserves the inputs behind the elevated research reports at `../../`.

`organization-manifest.md` records the exact elevate, sequester, quarantine, and delete decisions from the cleanup.

| Directory | Contents |
| --- | --- |
| `rawr-spec-landscape/` | Query, comparisons, audit files, and notes for the broad RAWR spec landscape audit. The elevated report is `../../spec-landscape-audit.md`. |
| `runtime-canon-arch-align/` | Query, comparisons, audit files, source analysis, and vendor verification notes for the runtime/architecture alignment report. The elevated report is `../../runtime-architecture-alignment-plan.md`. |
| `hyperresearch-inngest-proof/` | Hyperresearch Inngest proof inputs, blocked-proof remnants, source captures, claim traces, validation output, and parity-boundary notes. The elevated source report is `../../inngest-durable-workflow-findings.source-report.md`. |

The old stack-drain wrapper and draft drain plan were removed because they were transient process scaffolding. This directory keeps the durable evidence without preserving the obsolete process lane.
