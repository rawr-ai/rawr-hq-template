# Darwin Scratchpad

- Scope: Arc / `tsdkarc` fit.
- Main findings:
  - keep kernel design unchanged
  - describe `bootgraph` as RAWR derivative of Arc core lifecycle ideas
  - do not imply Arc already provides RAWR bootgraph API, identity model, or lifetime policy
  - remove `Required implementation work` from canonical boot spec body
