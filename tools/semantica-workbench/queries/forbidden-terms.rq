PREFIX sem: <https://semantica.dev/ontology/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?term ?label
WHERE {
  ?term a sem:ForbiddenPattern .
  OPTIONAL { ?term rdfs:label ?label . }
}
ORDER BY ?label
