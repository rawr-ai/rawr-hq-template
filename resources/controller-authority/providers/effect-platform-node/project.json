{
  "name": "provider-controller-authority-effect-platform-node",
  "projectType": "library",
  "sourceRoot": "resources/controller-authority/providers/effect-platform-node",
  "tags": [
    "type:provider",
    "layer:resource-provider",
    "resource:controller-authority",
    "provider:effect-platform-node"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"{projectRoot}/**/*.ts\""
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p {projectRoot}/tsconfig.json --noEmit"
      }
    }
  }
}
