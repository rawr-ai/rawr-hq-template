{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@rawr/cli",
  "targets": {
    "verify": {
      "executor": "nx:noop",
      "dependsOn": ["acceptance:oclif"]
    },
    "check": {
      "executor": "nx:noop"
    }
  }
}
