## 1. Grounding And Container Gate

- [x] 1.1 Attest exact C2 source/archive landing, canonical-main proof, completed tasks, and local Graphite/worktree drain in the archived C2 record.
- [x] 1.2 Bind the C3 record to accepted packet provenance and the superseding fully separate repository boundary; state the objective, hard core, exterior, falsifiers, authority ledger, proof boundary, protected lanes, and closure.
- [x] 1.3 Run the Nx first hop, inspect resolved legacy and landed-owner projects/graph, and classify current provider/promotion code as preserve, mine, or delete without importing the mixed aggregate.
- [x] 1.4 Complete the proposal, design, six capability deltas, tasks, and execution record; pass pinned strict OpenSpec and architecture baselines; resolve standing TypeScript, architecture/authority, behavior-first testing, and structural-quality findings before implementation.

## 2. Closed Lifecycle Modes

- [x] 2.1 Implement `ProviderDeploymentRequest = TargetedTest | CompleteTest | CanonicalSync` plus separate exact `CanonicalStatusRequest` and multi-target `ManagedRetireRequest` parsers in the provider-deployment owner with bounded canonical values and no optional alternate authority.
- [x] 2.2 Reject pairwise mixed selectors, unknown/path-shaped channels, canonical overrides, unsupported providers, noncanonical homes, and duplicate targets before constructing or calling any dependency.
- [x] 2.3 Add property/table tests for every legal mode and B33 violation class with zero-call traps across Git, artifact, provider, receipt, capsule, export, Oclif, app, and record ports.

## 3. Promotion And Governed Acceptance

- [x] 3.1 Create the `@rawr/agent-plugin-promotion` Nx service with bounded canonical schemas/digests for immutable mechanical-evidence observations/handles, `AcceptanceRequest`, issuer identity/protocol/schema/issuance-bound `AcceptanceEvidence`, policy, promotion attestation, and fixed channel records; promotion never generates evidence.
- [x] 3.2 Implement exact protected lifecycle path validation and the repository-governance verifier for issuer `independent-agent-plugin-acceptance/v1`, requested artifact, exact Git objects, transitive digest bindings, and a policy-authority hosted approval observation bound to the exact repository/ref/commit/tree/blob.
- [x] 3.3 Implement read-only release-input-equivalence verification across accepted source and landed-main Git identities without artifact publication, release rebuild, or provider rendering.
- [x] 3.4 Implement the fixed `current-main` resolver and disjoint repository states for content ahead, blocked acceptance authority, accepted pending convergence, current eligibility, stale/forged records, dirty/wrong/unreachable repositories, and lifecycle-record-only commits.
- [x] 3.5 Prove B12-B14 with canonical Git fixtures, altered-byte/squash/record-only cases, correct-policy-without-approval, approval-for-other-object, external-forgery and authority-unavailable cases, and mutation-port traps.

## 4. Immutable Provider Projection

- [x] 4.1 Create the `@rawr/agent-provider-deployment` Nx service and its consumer-owned `VerifiedReleaseReader` over pure release snapshot/ref types, plus branded projection/profile digests, versioned renderer/adapter protocol identities, bounded canonical encodings, closed provider identities, and exact issue/result unions.
- [x] 4.2 Implement Codex and Claude deterministic native renderers from verified artifact snapshots, one native plugin package per selected release member, and a closed renderer registry with no Cowork or generic-export fallback.
- [x] 4.3 Bind provider-visible files, modes, metadata, identities, renderer protocol, adapter protocol, release/set refs, and semantic capability predicates into projection identity while excluding homes, paths, mtimes, receipts, channels, and operation state.
- [x] 4.4 Prove B15-B16 and artifact-only behavior with order/path/time permutations, source deletion, tamper/missing artifact rejection, renderer/adapter protocol changes, changed-output reacceptance, byte-identical same-protocol refactor compatibility, and zero mutation calls.
- [x] 4.5 Extend the build/artifact owner with opaque immutable mechanical-evidence publish/read/retention adapters and bind provider-owned canonical evidence through a consumer-owned publisher; prove no-replace, tamper/missing, exact repeat, publication-failure, and accepted transitive-pin behavior.

## 5. Provider Targets, Inventory, And Receipts

- [x] 5.1 Define the closed target adapter protocol for capability inspection, live native plugin/skill/hook/enablement/marketplace inventory, mutation, visibility verification, retirement, and inverse classification at one canonical provider home.
- [x] 5.2 Implement bounded canonical target receipts with provider/home identity, generation/digest fencing, verified projection/member fingerprints, and a closed `TargetedTestScope | CompleteTestScope | CanonicalAcceptedScope` union; complete-test scope binds only target-local evaluation and evidence inputs, canonical scope records acceptance/promotion/channel settlement, the currently resolved accepted set alone supplies absent-member authority, and any prior same-target scope supplies ownership proof only.
- [x] 5.3 Extend the controller runtime layout with stable provider projection and target-state roots; implement digest-addressed verified projection materialization plus exact target-keyed sidecar receipts without ambient home discovery or worktree identity.
- [x] 5.4 Admit one exclusion-only target-identity sidecar as the first capsule-owned typed target action before native mutation; retain it across ordinary plugin retirement and expose the complete sidecar snapshot for C2 export overlap without granting convergence, receipt, or cleanup authority.
- [x] 5.5 Add target alias/deduplication, admission-then-failure/crash/first-operation-undo, ordinary-retire/undo retention, complete-snapshot malformed-entry, target isolation, copied/foreign receipt, stale receipt normalization, missing/altered live state, and live-read-every-run tests.

## 6. Native Provider Adapters

- [x] 6.1 Implement the Codex adapter's explicit-home capability probe, native marketplace/plugin install and enablement, live plugin/skill/hook plus effective plugin-configuration inventory, provider-visible verification, managed uninstall, and exact inverse operations behind injectable process/session ports.
- [x] 6.2 Implement the Claude adapter's explicit-home capability probe, native marketplace/plugin install and enablement, live cache/plugin/skill/hook plus effective enabled-plugin configuration inventory, provider-visible verification, managed uninstall, and exact inverse operations behind injectable process ports.
- [x] 6.3 Migrate only behavior-equivalent renderer/adapter mechanics from legacy evidence; add no source scan, direct projection, generic filesystem deletion, implicit home, Cowork provider, compatibility wrapper, or legacy import.
- [x] 6.4 Prove native single exposure, capability predicates, unmanaged marketplace/standalone collision blocking, native failure without fallback, and source-worktree independence in deterministic provider fixtures.

## 7. Deployment State Machine And Truthful Outcomes

- [x] 7.1 Implement read-only per-target inventory and deterministic plan for `ProviderDeploymentRequest`, `CanonicalStatusRequest`, and `ManagedRetireRequest` without a shared optional request bag, preserving omissions in targeted/complete tests and blocking test requests against canonical-receipt homes.
- [x] 7.2 Implement canonical-order multi-target apply/verify/retire with typed old-registration retire, member/config-residue absence verification, source transition, and install for a changed same-ID release; native replacement visibility before omitted-member cleanup; complete-set visibility before absent-member retirement; and receipt publication as each target's final mutation.
- [x] 7.3 Emit exhaustive target-scoped planned/applied/verified/retired/skipped/blocked/failed events and non-success partial outcomes that agree with actual state and preserve successful target truth.
- [x] 7.4 Implement the explicit B28 status decision table returning exactly one of `CONTENT_AHEAD_OF_ACCEPTANCE`, `ACCEPTED_PENDING_CONVERGENCE`, `CONVERGED`, `DRIFTED`, `BLOCKED_COLLISION`, or `INCOMPATIBLE_PROVIDER` without mutation or Oclif reads.
- [x] 7.5 Prove B17-B22/B28-B29 with inventory, install, visibility, cleanup, and receipt failpoints; targeted/complete omission preservation and canonical-scope blocking; stale-receipt-only normalization/write-failure/undo; managed/unmanaged/ambiguous ownership; A-success/B-failure; copied receipts; and per-phase state/event oracles.
- [x] 7.6 Prove a repeated converged operation performs required live reads and zero provider mutation, enablement, marketplace, filesystem, cleanup, rebuild, receipt, capsule, export, Oclif, or app-composition writes.
- [x] 7.7 Canonically emit aggregate mechanical evidence from immutable inputs and final verification/failure facts after target-local settlement, excluding timestamps and transaction history; publish it through the immutable artifact adapter, keep receipts independent of the aggregate digest, and prove complete success, partial A/B truth, same-digest publication failure/retry, source removal, and exact repeated evidence convergence.

## 8. Controller-Owned Provider Undo

- [x] 8.1 Implement the provider owner action/observation canonical codec, sequence grammar, target-binding selection, applying-recovery classifier, inverse executor, and prior verifier without a service-local capsule.
- [x] 8.2 Register the provider protocol in the CLI-owned closed capsule registry and bind one multi-target candidate to the exact applied prefix, including receipt as each target's final action.
- [x] 8.3 Prove exact reverse restore including action-owned first sidecar removal, cross-home/generation/digest rejection, ambiguous substitution blocking, later-target partial coverage, sidecar/provider/receipt crash recovery, retryable failures, ordinary-retire sidecar retention, and no capsule churn on read-only convergence (B25).

## 9. Inert Integration And Architecture Gates

- [x] 9.1 Add distinct minimal-port Template application factories for targeted test, complete test, canonical sync, canonical status, and managed retire, plus narrow CLI-root adapters for build reader -> `VerifiedReleaseReader`, build evidence store -> `MechanicalEvidencePublisher`, verified provider evidence -> promotion `MechanicalEvidenceReader`, promotion resolver -> `CanonicalChannelReader`, provider complete-home observation -> export-owned snapshot constructor/digest verifier, and provider owner protocol -> controller capsule without an umbrella lifecycle object, product orchestration, or discoverable command files.
- [x] 9.2 Add permanent dependency/module/export/activation gates enforcing the fixed provider/promotion domain-port-application-adapter topology, distinct application entrypoints and exact port sets (including no mutation/capsule/publisher ports in status), no public umbrella lifecycle object/barrel, curated root/subpath exports, sibling-only services over pure release, no inward adapter imports, no stateful service-to-service imports, narrow CLI composition-root mappings, C2 provider neutrality, legacy non-import, inert command/manifest surface, and personal/protected-path absence.
- [x] 9.3 Update only Template-owned internal contracts and C5 handoff notes required by C3; do not preserve old flags, aliases, aggregate terminology, or executable repository relationships.

## 10. Verification, Review, And Disposable Acceptance

- [x] 10.1 Run uncached build, typecheck, lint, test, structural, parser-property, Git-fixture, provider-state-machine, capsule, and architecture targets for every changed owner, then the exact Nx affected graph from the recorded C3 main base.
- [x] 10.2 Run generated-fixture and explicit disposable-home Codex/Claude acceptance for initial native visibility, changed same-ID retire with no member/config residue, source transition and reinstall, transition registration preserving omissions, explicit receipt-owned omitted-member retire with no residue, and final narrowed visibility; retain deterministic target/state-machine coverage for targeted/complete modes, unmanaged preservation, multi-home isolation, source deletion, partial truth, and second-run zero mutation; record exact evidence without canonical settlement.
- [x] 10.3 Prove that personal `c7375f52c79cf4c82a49cef51c7934fb7596a763` lacks the tracked C2 release input required for an authority-correct real-content build, perform zero mutation, and hand `cognition:state-machine-design` to C6 as the first real-content C2 build -> C3 targeted-test oracle after personal lands its governed closed-world release input and acceptance records.
- [x] 10.4 Cycle standing TypeScript/refactoring, architecture/authority, behavior-first testing, and structural-quality roles over the complete C3 slice; repair or evidence-disposition all packet-invariant, supported-behavior, and material-maintainability findings.
- [x] 10.5 Rerun pinned strict OpenSpec, permanent architecture gates, owner suites, integration acceptance, and exact affected graph after final review repairs with clean diff checks.

## 11. Landing And Closure

- [ ] 11.1 Commit and submit reviewed C3 source through Graphite with `--ai`, merge to canonical Template `main`, and rerun exact post-main OpenSpec, architecture, affected-graph, and disposable-home proof.
- [ ] 11.2 Synchronize the six truthful capability deltas and archive the completed C3 record only after source landing through the guarded OpenSpec path.
- [ ] 11.3 Land the archive record through Graphite, record exact reviewed/landed commits, trees, PRs, and post-main proof, then drain every C3 local Graphite node and disposable worktree before C5 depends on C3.
