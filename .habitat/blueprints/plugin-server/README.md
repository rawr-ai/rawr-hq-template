# Server Plugin

`plugin-server` is the container kind for plugins realized by a server host. It
has no enforced packet yet because the current server-plugin corpus does not
prove a universal source tree beyond the laws owned by narrower kinds.
