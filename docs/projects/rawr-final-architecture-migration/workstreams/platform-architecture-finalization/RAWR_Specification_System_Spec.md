# RAWR Specification System Spec Pointer

Status: non-authority workstream pointer.

The normative companion specification produced by this workstream lives at:

`docs/projects/rawr-final-architecture-migration/resources/spec/RAWR_Specification_System_Spec.md`

This pointer exists because the workstream plan originally named a workstream-local `RAWR_Specification_System_Spec.md` artifact. `docs/DOCS.md` defines project-scoped normative specs and guardrails as belonging under `docs/projects/<project>/resources/spec/`, so the authoritative spec was placed there.

Do not treat this pointer as a second copy of the specification.
