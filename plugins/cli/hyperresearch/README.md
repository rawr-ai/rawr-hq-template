# @rawr/plugin-hyperresearch

CLI projection for the `@rawr/hyperresearch-codex` service package.

Commands:

- `rawr hyperresearch codex-slice --query <query> --vault <path> --steps <path>`

This topic is the template-owned operator/testing surface. Codex skill, hook, MCP, and agent runtime material is currently synced from downstream RAWR HQ.
