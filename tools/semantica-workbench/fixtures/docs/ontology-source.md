# Ontology Fixture Source

## Core Concepts

Services own semantic capability truth in this synthetic workbench fixture.

## Constraints

The fixture rejects root-level `core/` and `runtime/` authoring roots and role-specific public start verbs.

## Verification Policy

Harness selection, graph law, proof-band, and merge-gating language exercise subordinate policy handling.

## Candidates

A constraint graph remains a candidate and cannot be promoted by generated evidence.
