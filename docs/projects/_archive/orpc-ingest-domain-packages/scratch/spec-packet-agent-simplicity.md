# Agent Scratch: Simplicity and Legacy Removal

- Remove runtime semantics from `templateRole` and `channel`.
- Deprecate then remove `publishTier`/`published` semantics.
- Eliminate dual composition paths in written architecture.
