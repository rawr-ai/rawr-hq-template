# Architecture Change Proposal Fixture

Services must own semantic capability truth for developer capability decisions.

Plugins should project capability through the plugins/agent/tools capability lane.

The runtime realization should bind resources before provider selection.

The runtime lifecycle should start through deterministic lifecycle phases.

Providers should implement resource contracts but must not become service truth.

Create a root-level `core/` authoring root for shared platform machinery.

Testing MUST preserve graph law and proof-band ratchets.

The classifier rule pack should emit a constraint graph artifact.

Should the agent tool gateway own semantic capability truth directly?

Add a topology read model under the diagnostics extension slot.

Add a comparison vocabulary extension slot for proposal review.

Propose an unlabeled orchestration thingy for later review.
