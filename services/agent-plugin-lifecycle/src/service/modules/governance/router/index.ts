import { currentMainRecord } from "./current-main-record.router";
import { currentMainSelection } from "./current-main-selection.router";

export const router = Object.freeze({
  currentMainRecord,
  currentMainSelection,
});
