/**
 * @fileoverview Package router export for `@rawr/coordination/router`.
 *
 * @remarks
 * Keep this file as the stable router import surface. The authoritative router
 * lives in `./service/router.ts`.
 */
export { router, type Router } from "./service/router";
