{
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "name": "runtime-substrate-effect",
  "root": "packages/core/runtime/substrate/effect",
  "sourceRoot": "packages/core/runtime/substrate/effect/src",
  "projectType": "library",
  "tags": ["type:runtime", "capability:habitat", "role:runtime-substrate-effect"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "bunx tsc -p packages/core/runtime/substrate/effect/tsconfig.json --noEmit",
          "bunx tsc -p packages/core/runtime/substrate/effect/tsconfig.test.json --noEmit"
        ]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "parallelism": false,
      "options": {
        "command": "bun test packages/core/runtime/substrate/effect/test"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsdown --config packages/core/runtime/substrate/effect/tsdown.config.ts"
      }
    },
    "check": {
      "executor": "nx:noop"
    }
  }
}
