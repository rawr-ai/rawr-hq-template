/**
 * @fileoverview Shared ORPC boundary error definitions for the HQ Ops package.
 *
 * @remarks
 * U02 is reservation-only, so this file intentionally exports no boundary
 * errors yet. Keep reusable caller-actionable ORPC errors here once later
 * slices add real HQ Ops procedures.
 */
export {};
