#!/usr/bin/env bun

console.error(
  "[phase-2_5 scaffold] verify-closure-allowlist.mjs is a Slice 0 gate placeholder. Implement the closure allowlist contract in Slice 6 before using this gate as acceptance evidence.",
);
process.exit(1);
