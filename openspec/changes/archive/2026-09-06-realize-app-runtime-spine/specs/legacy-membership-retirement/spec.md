## REMOVED Requirements

### Requirement: Legacy web membership commands are structurally absent (B04, B30)

**Reason**: The private Rawr app, its mixed web/scaffold commands, and every compatibility route are deleted during repository separation. Their one-time retirement record is not enduring Habitat capability law.

**Migration**: Preserve the accepted absence in the archived change and the finite product-separation oracle. External extensions use native `habitat plugins`; curated lifecycle uses the separately specified `habitat agent plugins` topic. No replacement web/scaffold path is created.

### Requirement: Repository state cannot own runtime membership (B05, B30, I08, I15)

**Reason**: The predecessor repository-state schema, app roots, server/web hosts, and readers are deleted. A permanent legacy-state capability would preserve obsolete product ontology in Habitat.

**Migration**: Runtime membership is authored only through the canonical Habitat app definition, selection, derivation, compilation, mounting, and observation contracts. The archived retirement record preserves why predecessor state is never imported.

### Requirement: Workspace discovery cannot recreate membership (B05, B30)

**Reason**: The predecessor server/web discovery and module-serving roots are deleted, while final app membership is governed positively by the Habitat runtime specifications.

**Migration**: Prove predecessor paths and readers absent at separation; prove final selected membership through the app/runtime owner when that vertical lands. Add no compatibility discovery rule.

### Requirement: Retirement does not create a replacement runtime authority (B05, B30)

**Reason**: The final Habitat runtime is realized by this change, so a canonical requirement that indefinitely defers it and names the old C4 migration phase becomes false authority.

**Migration**: Use the `app-runtime-realization` and `runtime-harness-boundary` capabilities added by this change. Preserve the old non-replacement decision only in the archived retirement record.

### Requirement: Retirement preserves adjacent authorities without entering them (B05, I08)

**Reason**: The one-time Rawr retirement boundary is complete; enduring external-extension, lifecycle, package-output, provider, and content-record ownership now lives in their positive canonical capabilities.

**Migration**: Validate those final owners through their own contracts and acceptance targets. Retain no Rawr CLI-version or protected-lane vocabulary as active Habitat law.
