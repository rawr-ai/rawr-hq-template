# @rawr/plugin-mfe-demo

Sample workspace runtime plugin shipped by `RAWR HQ-Template`.

## Local usage

```bash
bun run rawr -- hq plugins enable mfe-demo --risk off
bun run rawr -- hq plugins status --json
bun run rawr -- dev up
```

Then open `/mounts` in the web app.

