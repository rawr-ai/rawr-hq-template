{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "plugin-api-state",
  "root": "plugins/api/state",
  "sourceRoot": "plugins/api/state/src",
  "tags": [
    "type:plugin",
    "migration-slice:structural-tranche",
    "role:api",
    "surface:orpc",
    "capability:state"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc -p plugins/api/state/tsconfig.json --noEmit"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"plugins/api/state/**/*.{js,jsx,ts,tsx}\""
      }
    },
    "sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run sync:check --project plugin-api-state"
      }
    },
    "structural": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun scripts/phase-03/verify-plugin-api-state-structural.mjs"
      }
    }
  }
}
