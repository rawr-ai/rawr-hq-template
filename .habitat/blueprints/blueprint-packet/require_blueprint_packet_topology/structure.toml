schemaVersion = 1

[[scopes]]
name = "generic-blueprint-rule-packets"
root = ".habitat/blueprints/!(*.*)/!(*.*)"
kind = "directory"
mode = "closed"
required = [
  "baseline.json",
  "rule.json",
]
allowed = [
  "check.mjs",
  "pattern.md",
  "structure.toml",
]
