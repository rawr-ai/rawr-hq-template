{
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "name": "runtime-process-runtime",
  "root": "packages/core/runtime/process-runtime",
  "sourceRoot": "packages/core/runtime/process-runtime/src",
  "projectType": "library",
  "tags": ["type:runtime", "capability:habitat", "role:runtime-process-runtime"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "bunx tsc -p packages/core/runtime/process-runtime/tsconfig.json --noEmit",
          "bunx tsc -p packages/core/runtime/process-runtime/tsconfig.test.json --noEmit"
        ]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "parallelism": false,
      "options": {
        "command": "bun test packages/core/runtime/process-runtime/test"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsdown --config packages/core/runtime/process-runtime/tsdown.config.ts"
      }
    },
    "check": {
      "executor": "nx:noop"
    }
  }
}
