# Retired Workflow UI Operations Note

Status: retired. This file remains only as a tombstone for the former workflow UI operations surface.

Do not use this file as an active runbook. Current local runtime operations are documented in `docs/process/runbooks/HQ_RUNTIME_OPERATIONS.md`.

Preserved lesson: runtime workflow ingress and local HQ lifecycle checks are separate concerns. Keep validating supported Inngest and workflow runtime surfaces through the HQ runtime runbook and active server tests; do not restore removed UI, RPC, CLI, or file-storage guidance from this tombstone.
