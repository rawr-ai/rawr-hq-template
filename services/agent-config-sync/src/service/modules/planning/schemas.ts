/**
 * Planning has no durable entity schemas.
 *
 * Procedure input/output schemas live in `contract.ts`; this file exists only
 * to preserve the canonical module file shape required by structural ratchets.
 */
