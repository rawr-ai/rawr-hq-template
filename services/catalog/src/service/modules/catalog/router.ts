import { check } from "./router/check.js";
import { resolve } from "./router/resolve.js";

/** Composes the completed catalog operations into the module router face. */
export const router = { resolve, check };
