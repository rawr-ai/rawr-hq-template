## REMOVED Requirements

### Requirement: The mixed executable owners are absent

**Reason**: The mixed lifecycle owners and the separately owned create command are deleted; retained lifecycle behavior is already governed by the qualified Habitat service, resource, provider, and topic capabilities. This one-time migration inventory is not a permanent kind.

**Migration**: Preserve exact predecessor absence in the archived change and cumulative separation oracle. Use `agent-plugin-lifecycle-service-topology`, final authoring law, and owner-local contracts for retained behavior; create has no Habitat replacement.

### Requirement: Legacy commands, flags, and state claims are unreachable

**Reason**: Repository separation deletes the Rawr command application, compatibility flags, state, docs, and readers. Keeping their retired command grammar as canonical Habitat capability law would retain the product ontology being removed.

**Migration**: The archived record and finite absence target preserve the rejection evidence. Final command ontology is specified positively by `agent-plugin-command-lifecycle` and `external-cli-extension-boundary`.

### Requirement: Distribution and state authorities remain disjoint

**Reason**: The durable ownership distinction survives, but it is already owned positively by channel selection, release product, packaging, provider, resource, and external-extension capabilities rather than by a mixed-lifecycle retirement capability.

**Migration**: Apply those positive canonical requirements and archive this duplicate transition container. No aggregate repair authority or parallel lifecycle store is introduced.

### Requirement: Legacy app mounting remains retired without replacement

**Reason**: This change realizes the canonical Habitat app/runtime path, so the old requirement forbidding runtime realization and deferring it to a future migration directly contradicts the accepted target.

**Migration**: Preserve predecessor web/mounting absence in the separation oracle, and govern the realized replacement only through `app-runtime-realization`, `runtime-harness-boundary`, and their owner-local laws.
