{
  "name": "@rawr/resource-clock",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.7",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@rawr/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"**/*.{js,jsx,ts,tsx}\"",
    "sync": "bun run --cwd ../.. sync:check --project @rawr/resource-clock",
    "structural": "bun ../../scripts/phase-03/run-structural-suite.mjs --project @rawr/resource-clock",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project resource-clock"
  },
  "nx": {
    "tags": [
      "type:resource",
      "capability:clock",
      "migration-slice:runtime-production"
    ]
  }
}
