# `rawr tools …`

- Exports a small, curated catalog of known CLI commands (used by snapshots and UX).
- Keep this list stable and high-signal (avoid dumping every internal dev helper).

## Next
- `export.ts` — the catalog source of truth
- `../routine/snapshot.ts` — consumes `tools export`

