import { detect } from "./router/detect";
import { extract } from "./router/extract";

/** Composes the completed transcript operations into the module router face. */
export const router = {
  detect,
  extract,
};
