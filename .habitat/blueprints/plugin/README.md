# Plugin

`plugin` is the broad plugin kind. It has no enforced packet yet: the current
corpus does not prove one whole structural predicate shared by every plugin.
Narrower plugin kinds own only the additional laws they can establish.
