{
  "name": "provider-source-inventory-git-effect-platform-node",
  "projectType": "library",
  "sourceRoot": "resources/source-inventory/providers/git-effect-platform-node",
  "implicitDependencies": ["@habitat/resource-source-inventory"],
  "tags": [
    "type:provider",
    "layer:resource-provider",
    "resource:source-inventory",
    "provider:git-effect-platform-node"
  ],
  "targets": {
    "check": {
      "executor": "nx:noop"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p {projectRoot}/tsconfig.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test {projectRoot}/test"
      }
    }
  }
}
