{
  "name": "provider-agent-provider-records-effect-platform-node",
  "projectType": "library",
  "sourceRoot": "resources/agent-provider-records/providers/effect-platform-node",
  "tags": [
    "type:provider",
    "layer:resource-provider",
    "resource:agent-provider-records",
    "provider:effect-platform-node"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p resources/agent-provider-records/tsconfig.build.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"{projectRoot}/**/*.ts\""
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p {projectRoot}/tsconfig.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test {projectRoot}/test"
      }
    }
  }
}
