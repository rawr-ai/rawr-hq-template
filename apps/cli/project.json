{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@rawr/cli",
  "targets": {
    "verify": {
      "executor": "nx:noop",
      "dependsOn": ["test-oclif-boundary"]
    },
    "check": {
      "executor": "nx:noop"
    }
  }
}
