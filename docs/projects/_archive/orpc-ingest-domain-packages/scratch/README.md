# Archived Scratchpads

This directory preserves older scratchpads that were previously living under
the worktree root `.scratch/`.

They are archived here so the details remain available for lineage, while the
active worktree no longer exposes them as current orientation material.

Do not route active implementation or design work through these files.
Use the current authoritative docs instead:

- `../../../orpc-ingest-domain-packages/README.md`
