# ORPC Ingest Servicepackages

This directory contains the current working docs for the `example-todo`
golden-example / servicepackage scaffolding thread.

Role: packet router

Directory-name note:

- The packet directory name is retained for link stability.
- The live architecture described inside this packet is the current
  `servicepackage` model rooted in `services/*`.

## Start Here / Authority Map

Read these first when you need current truth for the active design and
integration conversation:

1. `ACTIVE_GROUNDING.md`
2. `DECISIONS.md`
3. `guidance.md`
4. `examples.md`
5. `DESIGN.md`
6. `TELEMETRY_DESIGN.md`
7. `ADAPTER_POSTURE.md`

Supporting/reference docs:

- `TELEMETRY_MIGRATION_IMPLEMENTATION_PLAN.md`
- `ADAPTER_AGENT_WORKFLOW.md`
- `ADAPTER_ORCHESTRATION_WORKFLOW.md`
- `SERVICE_CONTEXT_SEMANTICS_MINI_SPEC.md`

Use them when needed, but do not treat them as the first place to reason from
for the current adapter/host/service-input design knot.

## Historical Docs

Historical or superseded docs for this thread live under:

- the corresponding packet under `../_archive/`

Those files are kept for traceability only. They may describe earlier topology,
context, or planning directions that are no longer authoritative.

Reading rule:

- `ACTIVE_GROUNDING.md` = current worker brief
- `DESIGN.md` = structural seam/axis map
- `DESIGN.md` = structural seam/axis map for the remaining unresolved design work
- `DECISIONS.md`, `guidance.md`, and `examples.md` = durable core trio
