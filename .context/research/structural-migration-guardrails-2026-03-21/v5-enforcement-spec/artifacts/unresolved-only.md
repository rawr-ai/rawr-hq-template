# Unresolved Only

- Exact tag spellings inside the required axes remain implementation detail.
- Exact `depConstraints` factoring remains implementation detail.
- Exact sync-generator and project-graph plugin implementation remains open.
- Exact public API and internal decomposition of `packages/bootgraph` remains open.
- Exact landing package for extracted Inngest auth support matter remains open.
- The threshold for promotion from runtime-specific composition into a composed service remains open.

Everything else needed for the enforcement spec is settled by canon, the decisive guardrails proposal, and V5.
