# M2 Runtime Realization Lock Spike Context

This directory is project context for the runtime-realization lock spike and the final integrated canonical architecture handoff. It is not a repo-wide documentation cleanup area.

## First Hop

Use this packet for the next Cloud Pro handoff:

```text
integrated-canonical-architecture-finalization/cloud-pro-inputs/
```

That folder is the active five-file input packet for producing the final integrated canonical architecture document.

The supporting decision record for that handoff is:

```text
integrated-canonical-architecture-finalization/decision-record/
```

## Context Map

| Path | Status | Role |
| --- | --- | --- |
| `integrated-canonical-architecture-finalization/` | Active | Final integrated canonical architecture Cloud Pro input packet plus the comparison/modification decision record behind it. |
| `runtime-realization-finalization/` | Complete, not active handoff | Runtime Realization System finalization packet, review record, and supporting synthesis inputs. The promoted runtime authority now lives in `resources/spec/`. |
| `integrated-architecture-alignment/` | Complete, superseded by the finalization packet | Earlier alignment packet used before the Version B baseline decision. Keep as provenance and source context only. |
| `target-authority-reframe/` | Complete evidence record | Target-authority reframe and reviewer reports that corrected the runtime-realization posture. |
| `past-session-workflows/` | Archived provenance | Older workflow drafts retained for history only. |

## Authority Notes

- The active architecture handoff is `integrated-canonical-architecture-finalization/cloud-pro-inputs/`.
- The runtime overlap authority for that handoff is the copied file inside the active packet, sourced from `docs/projects/rawr-final-architecture-migration/resources/spec/RAWR_Effect_Runtime_Realization_System_Canonical_Spec.md`.
- Older Cloud packets in this directory are not active handoff inputs unless a later workflow explicitly reactivates them.
- Scratch outputs under sibling `.scratch/` paths are working memory, not authority.
- The separate `doc-preclean-sweep/` context at the parent level belongs to the broader documentation cleanup lane and is not part of this handoff packet.
