# Agent Plugin CLI Topic

## Scope
This private topic projects exactly check, package, status, sync, test, and vendors update.

## Boundaries
- Native Oclif owns one parse, mode relationships, scalar admission and bounded stdin transport.
- The public lifecycle client owns aggregate validation, content policy and native convergence.
- `services.ts` declares one shared service use. The app owns membership, profiles and providers.
- Never start a runtime, construct providers, import service internals, infer homes or retain a client here.
- Preserve service-owned byte failures and exact successful record bytes. No stdin cache or second parse.
- This topic owns the qualified root `.habitat/overlays/agent-plugin-lifecycle` policy; the generic CLI-topic blueprint remains shared and immutable.

## Validation
Run this project's focused typecheck and Bun tests, including native parser and overlay perturbation tests, then the app-owned installed lifecycle matrix.
