{
  "name": "@rawr/plugin-hyperresearch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist",
    "oclif.manifest.json",
    "README.md",
    "package.json"
  ],
  "oclif": {
    "commands": "./dist/commands",
    "topicSeparator": " ",
    "typescript": {
      "commands": "./src/commands"
    }
  },
  "dependencies": {
    "@oclif/core": "^4.8.0",
    "@rawr/core": "workspace:*",
    "@rawr/hyperresearch-codex": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "oclif": "4.23.27",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "manifest": "bun --bun oclif manifest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck-tests": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run --project plugin-hyperresearch",
    "lint": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"**/*.{js,jsx,ts,tsx}\""
  },
  "nx": {
    "tags": [
      "type:plugin",
      "migration-slice:structural-tranche"
    ],
    "targets": {
      "typecheck": {
        "dependsOn": [
          "typecheck-tests"
        ],
        "inputs": [
          "production",
          "^production",
          "typescriptRuntime"
        ]
      }
    }
  }
}
