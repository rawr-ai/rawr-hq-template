{
  "name": "@rawr/plugin-workspace",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.7",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "devDependencies": {
    "@types/bun": "^1.2.10",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"**/*.{js,jsx,ts,tsx}\"",
    "sync": "bun run --cwd ../.. sync:check --project @rawr/plugin-workspace",
    "structural": "bun ../../scripts/phase-03/run-structural-suite.mjs --project @rawr/plugin-workspace",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project plugin-workspace"
  },
  "nx": {
    "tags": [
      "type:package",
      "migration-slice:structural-tranche",
      "role:support",
      "surface:plugin-workspace"
    ]
  }
}
