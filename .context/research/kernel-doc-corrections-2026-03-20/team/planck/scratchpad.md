# Planck Scratchpad

- Scope: repo-reality fit under locked-direction posture.
- Main findings:
  - no repo detail forces kernel redesign
  - docs must stop sounding like `apps/hq/*` and role-first plugin layout are already present repo facts
  - host mount-time runtime augmentation around manifest-owned bundles should be made explicit
  - service examples should not accidentally lock one package/export shape unless intended
