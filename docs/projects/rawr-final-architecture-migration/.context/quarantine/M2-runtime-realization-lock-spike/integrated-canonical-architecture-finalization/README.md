# Integrated Canonical Architecture Finalization

Status: active handoff context.

Use `cloud-pro-inputs/` as the final curated Cloud Pro packet for producing the standalone integrated canonical architecture document. The packet must remain exactly five input files.

Use `decision-record/` only as supporting provenance for how the packet was derived: Version B / Alt-X-2 is the baseline after bounded edits, Version A / Alt-X-1 is the transplant source, and migration planning can begin after the bounded repairs captured in the packet.

Do not mix in the earlier alignment packet, old candidate reports, scratch outputs, current implementation reality, or migration planning docs.
